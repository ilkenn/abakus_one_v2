# Firestore Data Model — Phase 9 (`docs/decisions.md` ADR-026)

**Status: foundation, partially implemented.** This document describes the collection strategy
`firestore.rules` enforces and the pilot repository migration (Sprint 9E) targets. It is written
against the real, provisioned Firebase projects (`abakusone`/`abakus-one-dev`/`abakus-one-staging`)
but has **not been deployed to any of them** — everything here is emulator-verified only until an
explicit deployment step (outside this sprint's scope, per the Phase 9 kickoff's own stop conditions)
runs `firebase deploy --only firestore:rules`.

## Isolation model

**Shared project, shared collections, mandatory `organizationId`.** Per the Phase 9 architecture
analysis (`docs/phase9_architecture_analysis.md` §6), this is the initial model — cheapest, fastest to
ship, matches every existing Dart repository's own `organizationId`-as-field shape. A future enterprise
tier isolating a tenant into its own Firestore database/project is an explicitly deferred upgrade path,
not built here.

**Every tenant-owned document carries `organizationId` directly (denormalized), not only through a
parent-document lookup chain.** This is a deliberate performance/auditability choice: a rule that had
to `get()` branch → restaurant → organization on every single read would be slow and easy to get wrong.
Instead:
- `organizationId` is denormalized onto every tenant-owned document (branches carry it directly, not
  only via `restaurantId`).
- **Enforcement of the real chain happens at write time, not read time**: `firestore.rules`' `create`
  rules verify the denormalized `organizationId` actually matches the resolved parent (branch's
  `restaurantId` → that restaurant's real `organizationId`) via `get()`, and `update` rules make
  `organizationId` **immutable** after creation (no rule permits changing it). This is exactly what the
  kickoff's "branch scope resolves through branch → restaurant → organization" requirement means in
  practice: verified once, trusted (and fast) thereafter — not re-verified on every read.

## Custom claims vs. Firestore lookups

Per-request Firestore `get()` calls inside a security rule are real reads (they count against quota and
add latency) — the standard, recommended pattern for a Firestore-backed multi-tenant app is **Firebase
Auth custom claims** as the fast path, with Firestore as the durable source of truth a Cloud Function
syncs into claims whenever it changes. This document specifies both halves:

- `memberships/{organizationId}_{uid}` is the **source of truth** — created/updated only by trusted
  Cloud Functions (Faz R.3A, `functions/src/staffMembership.ts`: `bootstrapFirstAdminAccount`,
  `registerStaffMember`, `assignStaffRole`/`revokeStaffRole`, `grantStaffBranchAccess`/
  `revokeStaffBranchAccess`, `setStaffMemberStatus`), never directly by a client.
- `request.auth.token.organizationAccess` (a custom claim, a list of organization ids) and
  `request.auth.token.roles` (a map of `organizationId -> [roleNames]`) are the **fast path** rules
  check first. **Closed, Faz R.3A — with a different mechanism than originally sketched here.** This
  document originally envisioned an automatic Firestore *trigger* on `memberships` writes; what was
  actually built is a **client-invoked callable**, `syncOwnStaffClaims`
  (`functions/src/staffMembership.ts`) — the signed-in caller calls it themselves (derives claims
  entirely from their own *active* `memberships` documents, no client input trusted), then force-
  refreshes their ID token (`getIdToken(true)`) before the new claims take effect. Every mutating
  callable above also re-syncs the *affected* uid's claims server-side as part of the same call, so a
  staff member never needs to separately ask "did my role change take effect yet" — but the *caller's
  own client-side token* still only updates on their own next `syncOwnStaffClaims` + refresh (an
  inherent property of Firebase custom claims, not a gap specific to this implementation).
  `FirebaseStaffAuthRepository.signIn` (`lib/features/admin/data/staff_auth_repository.dart`) calls
  `syncOwnStaffClaims` + refreshes automatically right after every real sign-in, so this needs no
  manual step in the ordinary admin sign-in flow.
- Platform-level authorization uses a **wholly separate claim**, `request.auth.token.platformRole` —
  never the same claim namespace as tenant `roles`/`organizationAccess`, mirroring
  `PlatformActorSession`'s existing client-side "zero shared types with the tenant stack" design
  (`docs/decisions.md` ADR-025).

## Collections

| Collection | Key | Tenant field | Written by | Notes |
|---|---|---|---|---|
| `platformMembers/{platformMemberId}` | sequential id | — (platform-owned) | Cloud Function only | Mirrors `PlatformMember`. Client: read own doc only. |
| `organizations/{organizationId}` | caller-supplied deterministic id (Faz D.1.1 — mirrors `restaurants`/`branches`' own convention below; corrects this row's earlier "org id" description, which no implementation ever existed to produce) | is the boundary | Cloud Function only — `provisionOrganization` (`functions/src/provisionOrganization.ts`, Faz D.1.1), Platform Owner/Administrator-only | Mirrors `Organization`. Client: read if a member. Idempotent upsert — no parent to bind against, so `name`/`isActive` may be legitimately re-provisioned (unlike `restaurants`/`branches`, there is no "immutable binding" concept here, since the document's own id *is* the tenant boundary). `isActive` (boolean, default `true`) is Firestore-canonical-only for now — the `Organization` Dart domain model has no status field yet. |
| `restaurants/{restaurantId}` | caller-supplied deterministic id (Faz D.1 — mirrors `Order.id`'s own external-id/idempotent-`.set()` convention, not a Firestore auto-id; corrects this row's earlier "sequential id" description, which no implementation ever existed to produce) | `organizationId` | Cloud Function only — `provisionRestaurant` (`functions/src/provisionRestaurant.ts`, Faz D.1/D.1.1), Platform Owner/Administrator-only | Mirrors `Restaurant`. Idempotent upsert; `organizationId` immutable once first set. **Faz D.1.1**: `organizationId` is no longer validated structurally only — `provisionRestaurant` reads `organizations/{organizationId}` server-side, inside the same transaction, and requires it to both exist and have `isActive == true` before writing. Closes Faz D.1's one REQUIRED finding. |
| `branches/{branchId}` | caller-supplied deterministic id (Faz D.1, same convention as `restaurants` above) | `organizationId` (denormalized, verified against `restaurantId`'s real owner at create) | Cloud Function only — `provisionBranch` (`functions/src/provisionBranch.ts`, Faz D.1), Platform Owner/Administrator-only | Mirrors `Branch`. The branch -> restaurant -> organization chain described in the "Required guarantees" table below is now a real, tested mechanism, not just a documented intent: `provisionBranch` reads its parent `restaurants/{restaurantId}` document server-side, inside the same transaction, and rejects if that document doesn't exist or its `organizationId` doesn't match the branch's own claimed value. Idempotent upsert; `organizationId`/`restaurantId` immutable once first set. |
| `menuCategories/{categoryId}` | caller-supplied deterministic id (matches the real Dart `MenuCategory.id`, e.g. `cat_bowl`) | `organizationId`/`restaurantId` | Admin SDK only — `functions/src/catalogMigration.ts`'s `migrateCanonicalCatalog`, Faz D.3.1 | Firestore-canonical representation of `MenuCategory`. Fields: `organizationId`, `restaurantId`, `name`, `sortOrder`, `isActive`. |
| `menuProducts/{productId}` | caller-supplied deterministic id (mirrors `Order.id`'s convention — matches the existing `MenuProduct.id` string identity, e.g. `prod_mexifit_bowl`) | `organizationId`/`restaurantId` | Admin SDK only — no callable write path (Faz D.3); **Faz D.3.1**: the real, full catalog (7 categories/79 products/64 bowl ingredients) is now generated deterministically from the real Dart `AbakusMenuCatalog`/`LocalBowlBuilderCatalogRepository` via `tool/export_menu_catalog.dart` → `functions/scripts/data/menu_catalog_export.json` → `functions/src/catalogMigration.ts`'s `migrateCanonicalCatalog` (idempotent, batched `.set()` upsert); `scripts/migrate_canonical_catalog.mjs` is the real migration script, `scripts/seed_dev_catalog.mjs` is now a superseded ~5-product representative-fixture seed (its ids are overwritten once the real migration runs) | Firestore-canonical representation of `MenuProduct`/`ModifierGroup`/`ModifierOption`/`ChannelPriceRule` (not a new, parallel model). Fields: `organizationId`, `restaurantId`, `categoryId`, `name`, `isAvailable`, `basePriceMinorUnits`, `modifierGroups` (embedded array, each with nested `options` — empty for every real product today, since the real menu has none), `channelPriceOverrides` (map keyed by channel — empty for every real product today). Prices are integer TRY minor units, never a float. Dart is the single hand-authored source; Firestore is generated, never hand-edited — re-running the migration after any Dart catalog change keeps both in sync **by construction**, not automatically enforced (no reconciliation UI exists if a Dart edit ships without re-running `migrate:catalog` — RECOMMENDED operational finding, Faz D.3.1). Read by `submitTakeawayOrder`'s pricing engine only — no client reads this collection yet (the Flutter app still displays from its own compiled-in catalog). |
| `bowlIngredients/{ingredientId}` | caller-supplied deterministic id (matches the real Dart `BowlBuilderIngredient.id`, e.g. `bb_protein_izgara_tavuk` — Faz D.3.1; Faz D.3's superseded fixture used `bowl_ing_*` ids) | `organizationId`/`restaurantId` | Admin SDK only — same migration path as `menuProducts` | Firestore-canonical representation of `BowlBuilderIngredient`. Fields: `organizationId`, `restaurantId`, `categoryId`, `name`, `priceMinorUnits`, `isAvailable`. |
| `channelPricingPolicies/{restaurantId}` | the restaurant's own id (one policy document per restaurant) | `organizationId` (not stored on the doc itself — resolved via the parent restaurant) | Admin SDK only — **Faz D.3.1.1**: written by `catalogMigration.ts`'s `migrateCanonicalCatalog` as part of the same catalog migration `npm run seed:dev-all`/`migrate:catalog` already run, sourced from the real Dart `InMemoryChannelPricingPolicyRepository` via `tool/export_menu_catalog.dart`'s export — closes Faz D.3.1's REQUIRED finding (a fresh `seed:dev-all`-only bootstrap now has a working pricing policy, no extra step needed). `scripts/seed_dev_catalog.mjs`'s own separate, hand-typed write of the same document is superseded (marked in its own header, not deleted) — one canonical pricing seed, not two. | Firestore-canonical representation of `ChannelPricingPolicy`. Fields: `channelDefaultAdjustments` (map: channel -> minor units), `categoryOverrides` (map: channel -> categoryId -> minor units). Missing document = empty policy (every channel/category resolves to a zero adjustment), mirroring `InMemoryChannelPricingPolicyRepository`'s own "no policy set" fallback — never a thrown error. |
| `takeawayQrCodes/{qrCodeId}` | generated id | `organizationId`/`restaurantId`/`branchId` (own fields, independently re-verified against the real chain at resolution time — see `takeawayQrTokenResolution.ts`) | Admin SDK only — no callable write path at all this phase (Faz D.2); `scripts/seed_dev_takeaway_qr.mjs` is the dev/emulator seed tool | Fields: `opaqueToken`, `organizationId`, `restaurantId`, `branchId`, `status` (`active`/`inactive`/`rotated`/`expired`), `createdAt`, `updatedAt`, `expiresAt` (nullable), `revision`. No `tableId` — a takeaway QR belongs directly to a branch. Not client-readable at all (no match block; falls through to the fail-closed catch-all, same as `tableQrCodes`/`restaurantTables`). |
| `takeawayGuestSessions/{sessionId}` | generated id | `organizationId`/`restaurantId`/`branchId` (server-derived at create time, never client input) | Cloud Function only — `openTakeawayGuestSession` (`functions/src/openTakeawayGuestSession.ts`, Faz D.2) | Fields: `organizationId`, `restaurantId`, `branchId`, `guestAuthUid`, `status`, `createdAt`, `expiresAt`, `lastActivityAt`, `qrTokenId`. TTL: `takeawayGuestSessionConfig.ts`, 30 minutes by default (RECOMMENDED decision — see that file's own doc comment), overridable via `TAKEAWAY_GUEST_SESSION_TTL_MINUTES`. Client: owner-read-only (`guestAuthUid == request.auth.uid`), never write. Same-caller/same-token re-open reuses the existing active session rather than creating a duplicate. |
| `tableGuestSessions/{sessionId}` | generated id | `organizationId`/`restaurantId`/`branchId` (server-derived at create time, never client input) | Cloud Function only — `openTableGuestSession` (`functions/src/openTableGuestSession.ts`, Table Guest Session Phase 1/2/3; row previously missing from this table) | Fields: `organizationId`/`restaurantId`/`branchId`/`tableId`, `guestAuthUid`, `status` (`active`\|`closed`\|`revoked`\|`expired`), `createdAt`, `expiresAt` (TTL: `tableGuestSessionConfig.ts`, 6 hours by default), `lastActivityAt`, `qrTokenId`. **New field, Faz R.1C.2**: `reservationContextId` (nullable) — a server-generated, immutable snapshot of whichever `activeReservationTableContext` was live on this table at the exact moment the session was opened (`null` for the ordinary walk-in case); never changes after creation, never client-writable (this collection's own `allow write: if false` already covers it — no separate rule needed). Client: owner-read-only (`guestAuthUid == request.auth.uid`), never write. **New field, AP-3 Wave 1**: `tableSessionId` — the id of the NEW, table-level `tableSessions/{id}` document (below) this guest session belongs to; concurrency-safely created-or-reused by `openTableGuestSession` itself (the lock is `restaurantTables.activeTableSessionId`, see that row's own update). |
| `reservations/{reservationId}` | deterministic `sha256(uid\|submissionKey)` (Faz R.1A, mirrors `orders`' takeaway-path id-derivation convention) | `organizationId`, `branchId` | Cloud Function only — `submitReservation` (create), `respondToReservation`/`respondToProposedChange`/`reservationSweep` (status transitions, Faz R.1B); **Faz R.3B**: `cancelReservation`/`completeReservation`/`markReservationNoShow` (terminal transitions) | Fields: `organizationId`/`restaurantId`/`branchId`/`areaId`, `customerId` (always a real phone-verified uid — no anonymous reservations exist), `contactFirstName`/`contactLastName`/`contactPhone`, `partySize`, `requestedTime`, `requestedAreaId`, `requestedAvailabilityAtSubmission` (`'available'`\|`'unavailable'`), `status` (`'pendingRestaurantApproval'`\|`'changeProposed'`\|`'confirmed'`\|`'rejected'`\|`'cancelled'`\|`'completed'`\|`'noShow'` — the last two added Faz R.3B; `'cancelled'`/`'completed'`/`'noShow'` are all real, canonical terminal statuses now, none re-enterable), `activeHoldId`/`activeProposalId` (nullable), `confirmedTime`/`confirmedAreaId` (nullable, set on confirm — re-validated minute-aligned per Faz R.0.7 §2 both at direct confirm and at proposal-accept), `reasonCode`/`reason` (nullable, set on reject — `'restaurantResponseTimeout'` is the sweep's own reasonCode, not a distinct status), `respondedByStaffId` (nullable), `responseDeadlineAt`, `submissionFingerprint`, `createdAt`/`updatedAt`. **New field, Faz R.1D.1**: `preorderOrderId` (nullable, immutable once set) — the deterministic id (`` `reservation-preorder-${reservationId}` ``) of this reservation's optional linked preorder `orders` document, `null` if no preorder was submitted; every confirm/reject/sweep/cancel/complete/noShow path resolves the linked preorder this way, never from a client-supplied order id. **New fields, Faz R.3B**: `cancelledAt`/`cancelledBy` (`'customer'`\|`'staff'`)/`cancelledByStaffId` (nullable — only set for a staff cancellation, never fabricated for a customer's own)/`cancellationReasonCode`/`cancellationReason` (all nullable, set on cancel); `completedAt` (set on complete); `noShowAt` (set on no-show) — every timestamp is server time only, never a client-supplied instant. Client: owner (`customerId`) or org-member read; never write. |
| `reservationAreas/{areaId}` | caller-chosen deterministic id (dev seed: `garden`/`indoor`) | `organizationId`/`restaurantId`/`branchId` (own fields) | Admin SDK only — no callable write path (Faz R.1A); `scripts/seed_dev_reservation.mjs` is the dev/emulator seed tool | Fields: `organizationId`/`restaurantId`/`branchId`, `displayName`, `sortOrder`, `isActive`, `capacity`, `createdAt`/`updatedAt`/`revision`. Not client-readable at all this phase (no match block; fail-closed catch-all, same as `tableQrCodes`) — a later phase may open read access once real picker UI needs it. |
| `reservationPolicies/{branchId}` | the branch's own id (one policy per branch) | `organizationId`/`restaurantId`/`branchId` (own fields) | Admin SDK only — same as `reservationAreas`; `scripts/seed_dev_reservation.mjs` | Fields: `enabled`, `bookingHorizonDays`, `slotIntervalMinutes`, `reservationDurationMinutes`, `maxPartySize`, `customerCancellationCutoffMinutes`, `restaurantResponseTimeoutMinutes`, `proposalHoldMinutes`, `timezone`, `createdAt`/`updatedAt`/`revision`. `minimumAdvanceMinutes` is deliberately absent — it is `submitReservation.ts`'s own `MINIMUM_ADVANCE_MINUTES` platform constant, never a policy field. `enabled` is the sole authority for "does this branch accept reservations" (never `supportedOrderChannelIds`). Not client-readable this phase, same reasoning as `reservationAreas`. |
| `reservationHolds/{holdId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `submitReservation` (create, `purpose: 'initialRequest'`), `respondToReservation` (create `'alternativeProposal'` holds; consume/release), `respondToProposedChange`/`reservationSweep` (consume/release/expire) | Fields: `reservationId`, `organizationId`/`branchId`/`areaId`, `bucketIds` (the `reservationSlotOccupancy` documents this hold's capacity is reflected in), `partySize`, `purpose` (`'initialRequest'`\|`'alternativeProposal'`), `proposalId` (nullable, set only on `'alternativeProposal'` holds), `status` (`'active'`\|`'consumed'`\|`'released'`\|`'expired'` — every transition guarded by the caller checking `status=='active'` first, so a hold can never be released/consumed/expired twice), `expiresAt`, `createdAt`/`updatedAt`. Not client-readable at all — capacity accounting is purely a backend concern. |
| `reservationSlotOccupancy/{branchId}__{areaId}__{slotStartIso}` | deterministic composite string (Faz R.0.3's own bucket-architecture convention — mirrors `reservationTableOccupancy`'s future analogue) | `branchId`/`areaId` (own fields) | Cloud Function only — `submitReservation`/`respondToReservation`/`respondToProposedChange`/`reservationSweep`, `tx.set(..., {merge:true})` | Fields: `branchId`/`areaId`/`slotStart`/`capacity` (denormalized from the area at first write), `confirmedPartySize` (written starting Faz R.1B — confirm/accept convert held capacity into confirmed, or claim it directly when no hold applies), `heldPartySize`. `confirmedPartySize + heldPartySize <= capacity` holds at all times; decrements are hard-clamped to `Math.max(0, current - partySize)` inside the same transaction, never a blind decrement — Faz R.1B §14. One document per `slotIntervalMinutes`-sized time bucket a reservation's `[requestedTime, requestedTime+reservationDurationMinutes)` interval touches — deterministic, query-less (`tx.get()` per bucket id, never a Firestore query), so capacity checking participates fully in the transaction's optimistic-concurrency read-set. Not client-readable — a client could otherwise probe real-time capacity/occupancy data with no authorization check at all. |
| `reservationChangeProposals/{proposalId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `respondToReservation` (create), `respondToProposedChange` (accept/reject), `reservationSweep` (expire) — Faz R.1B; **Faz R.3B**: `cancelReservation` (terminalizes a still-`pendingCustomerResponse` proposal to `'cancelled'` when its parent Reservation is cancelled from `changeProposed`) | Immutable audit trail, never overwritten once resolved. Fields: `proposalId`, `reservationId`, `organizationId`/`restaurantId`/`branchId`, `fromTime`/`fromAreaId` (the reservation's own requested time/area at proposal time), `proposedTime`/`proposedAreaId`, `createdByStaffId`, `createdAt`, `customerResponseDeadlineAt` (`min(now + ReservationPolicy.proposalHoldMinutes, proposedTime)`), `status` (`'pendingCustomerResponse'`\|`'accepted'`\|`'rejected'`\|`'expired'`\|`'cancelled'` — the last added Faz R.3B, set only via the parent Reservation's own cancellation, never faked as `accepted`/`rejected`), `holdId` (the `'alternativeProposal'`-purpose hold backing this proposal's capacity claim), `respondedAt` (nullable), `cancellationReason` (nullable, Faz R.3B — always `'parentReservationCancelled'` today). Client: org-member read only (customer read deferred — no UI exists yet to consume it); never write. |
| `reservationEvents/{eventId}` | deterministic — `{reservationId}-{eventType}` for events that occur at most once per reservation (`confirmed`/`rejected`/`responseTimedOut`/**`cancelled`/`completed`/`noShow` (Faz R.3B)**), `{reservationId}-{eventType}-{proposalId}` for events scoped to a specific proposal (`changeProposed`/`changeAccepted`/`changeRejected`/`changeExpired`) | `organizationId`/`branchId` (own fields) | Cloud Function only — written by `respondToReservation`/`respondToProposedChange`/`reservationSweep`; **Faz R.3B**: `cancelReservation`/`completeReservation`/`markReservationNoShow` — **Faz R.1B.1**: `tx.set()` *inside* the same mutating transaction as the state transition it records (real atomicity — either both commit or neither does), not a separate post-commit step (Faz R.1B's own original design, corrected) | Outbox markers only — mirrors `orderEvents`' exactly-once *shape* (deterministic id) but is not itself written by a separate Firestore trigger (a before/after status diff on `reservations` can't always tell which action produced a given transition), and uses `.set()` rather than `.create()` (reasoned in `reservationEvents.ts`'s own doc comment — no retrigger source exists here, unlike `onOrderCompleted`'s trigger). Fields: `type` (one of the 10 named reservation event types, up from 7 — Faz R.3B added `reservationCancelled`/`reservationCompleted`/`reservationNoShow`), `reservationId`/`organizationId`/`restaurantId`/`branchId`, `recordedAt`, `delivered` (always `false` — this immutable audit field is never rewritten post-commit, by design; **Faz R.3C**: real push delivery now happens, but its state is tracked separately in the new `reservationNotificationDeliveries/{eventId}` sibling record so this record itself never needs a post-commit update), plus event-specific `extra` fields (e.g. `proposalId`/`reasonCode`). **New fields, Faz R.3B**: `actorType` (`'customer'`\|`'staff'`)/`actorId` (nullable — only set on the three new terminal-lifecycle events, via `writeReservationEvent`'s new optional `actor` parameter; never a role name or other custom-claims content). Not client-readable. |
| `restaurantTables/{tableId}` | caller-supplied deterministic id (e.g. `table-12`; existing since Faz D.2 — this row was previously missing from this table) | `organizationId`/`restaurantId`/`branchId` (own fields) | Admin SDK only — no callable write path exists (confirmed via research before Faz R.1C.1); `scripts/seed_dev_table_qr.mjs` is the dev/emulator seed tool | Fields: `organizationId`/`restaurantId`/`branchId`, `branchDisplayName`, `floorPlanId`, `displayName`, `areaName` (free-form venue-defined text, e.g. "Teras" — a deliberate, documented exception to "no free-form strings," `docs/table_qr_architecture.md`), `capacity`, `status` (`available`\|`occupied`\|`reserved`\|`cleaning`\|`disabled`), `sortOrder`, `isActive`, `createdAt`/`updatedAt`/`revision`. **New field, Faz R.1C.1**: `reservationAreaId` (nullable) — the canonical FK to `reservationAreas` this phase's own research confirmed did not exist anywhere before (`areaName` is free text, never a foreign key); additive and backward-compatible (no existing reader is affected — there was no callable write path to begin with). `assignReservationTable` fails closed (`failed-precondition`) if a table's `reservationAreaId` doesn't match the reservation's own `confirmedAreaId`, including when it's unset. The Dart `RestaurantTable` domain model is deliberately not updated this phase — no UI consumes this field yet. **New field, AP-3 Wave 1**: `activeTableSessionId` (nullable) — the id of the currently-active `tableSessions/{id}` document for this physical table, if any; this field itself IS the concurrency-safe open/reuse lock `openTableGuestSession` reads-then-writes inside one transaction (corrected AP-3 Stage A report §6.1) — two simultaneous first-scans of the same table can never both create a fresh `TableSession`, since Firestore retries the losing transaction against the winner's already-committed write. Cleared (`null`) only by a future table-close/transfer callable (not yet built — Wave 1 scope is open/reuse only, not close/transfer/merge). |
| `reservationTableOccupancy/{tableId}__{slotStartEpoch}` | deterministic composite string, epoch **milliseconds** (Faz R.1C.1 — mirrors `reservationSlotOccupancy`'s bucket-id convention, but keyed by table instead of area) | `organizationId`/`branchId` (own fields) | Cloud Function only — `assignReservationTable` (`src/reservationTableOccupancy.ts`) | Exclusive physical-table locking — deliberately separate from `reservationSlotOccupancy` (area capacity/party-size accounting): a confirmed Reservation may exist with no physical table assigned at all. Unlike the area-capacity buckets (counters, since several reservations can share one area's pool), a bucket here is a single exclusive lock — its mere *existence* (with `assignedReservationId` set) means the table is taken for that slot; releasing is `tx.delete()`, never a counter decrement. Fields: `organizationId`/`restaurantId`/`branchId`/`tableId`, `slotStart`, `assignedReservationId`. Deterministic, query-less (`tx.get()` per bucket id only — Faz R.1C.1's own explicit "no open-ended overlap query" requirement). Not client-readable by anyone but org staff. |
| `reservationTableProtections/{reservationId}` | the reservation's own id (one record per reservation, overwritten in place on reassignment) | `organizationId`/`branchId` (own fields) | Cloud Function only — `assignReservationTable` | QR reservation-time protection *data* only this phase — not a hot-path source, an audit + deterministic cleanup list for a later phase's actual QR-enforcement to consume (explicitly out of scope this phase). Fields: `reservationId`, `organizationId`/`restaurantId`/`branchId`, `tableId`, `protectionStartAt` (`confirmedTime - 20 minutes`, USER-LOCKED `PROTECTION_LEAD_MINUTES` platform constant, Faz R.0.4), `protectionEndAt` (`confirmedTime + reservationDurationMinutes`), `bucketIds` (the `reservationTableOccupancy` documents this assignment locked), `active`, `createdAt`/`updatedAt`. `confirmedTime` must satisfy the Faz R.0.7 §2 minute-alignment invariant — fail closed, never silently rounded. Org-staff read only. |
| `tableProtectionMinuteBuckets/{tableId}__{epochMinute}` | deterministic composite string, `epochMinute` = integer minutes-since-epoch (Faz R.1C.1) | `organizationId`/`branchId` (own fields) | Cloud Function only — `assignReservationTable`/`openReservationTable` (`src/reservationTableProtection.ts`) | One document per whole minute in `[protectionStartAt, protectionEndAt)` a table's protection window touches. Unlike the exclusive occupancy buckets above, **multiple reservationIds may share one bucket** (two non-overlapping bookings on the same table can have overlapping 20-minute protection windows) — `reservationIds` is an array (`FieldValue.arrayUnion`/`arrayRemove`, never a blind overwrite); removing the last id from the array deletes the document rather than leaving an empty array behind. `removeReservationTableProtection` is reused verbatim by both writers (Faz R.1C.1's own assignment/reassignment path and Faz R.1C.2's `openReservationTable`) — still not wired to any cancellation flow. **Faz R.1C.2**: this is now the real, server-authoritative QR T-20 enforcement source — `resolveTableQrToken`/`openTableGuestSession` read the current-minute bucket by deterministic id (`epochMinute = floor(serverNowMillis / 60000)`), no query, no scheduler. Fields: `organizationId`/`restaurantId`/`branchId`/`tableId`, `epochMinute`, `reservationIds`. Org-staff read only. |
| `activeReservationTableContext/{tableId}` | the table's own id (one context per table, overwritten in place across open/close cycles) | `organizationId`/`branchId` (own fields) | Cloud Function only — `openReservationTable`/`closeReservationTable` (`src/reservationTableContext.ts`) — Faz R.1C.2; **Faz R.3B**: `cancelReservation`/`completeReservation`/`markReservationNoShow` also deactivate a live context atomically with their own terminal transition | The operational "this physical table is currently open for reservation X" marker — deliberately not a business-outcome record (`Reservation.status` is never touched by open/close, though the three Faz R.3B terminal callables *do* deactivate a live context as part of their own transition). "Live" is a read-time invariant, not a stored guarantee: `active == true` **and** `serverNow < contextEndAt`, re-derived on every read (`readLiveReservationTableContext`) — no scheduler expires a stale context. Fields: `reservationId`, `organizationId`/`restaurantId`/`branchId`/`tableId`, `openedByStaffId`, `openedAt`, `contextEndAt` (copied from the opening reservation's own `reservationTableProtections.protectionEndAt` — never independently recomputed), `active`, `closedAt` (nullable), `closedByStaffId` (nullable — the real staff uid for a manual `closeReservationTable` call or a staff-performed Faz R.3B terminal action; always `null` for a customer's own cancellation, never fabricated), `updatedAt`. **New field, Faz R.3B**: `closeReason` (nullable — `'reservationCancelled'`\|`'reservationCompleted'`\|`'reservationNoShow'`, set only by the three new terminal callables; unset/`null` for a manual `closeReservationTable` call, distinguishing an operational close from a terminal-lifecycle one). Read by `openTableGuestSession` (to snapshot `reservationContextId` onto a new session) and `assignReservationTable` (to hard-fail a reassignment while live). Org-staff read only; customers never read this directly. |
| `tableSessions/{tableSessionId}` | generated id | `organizationId`/`restaurantId`/`branchId` (own fields) | Cloud Function only — `openTableGuestSession` (create-or-reuse, concurrency-safe via `restaurantTables.activeTableSessionId`), `submitDineInOrder` (staffEntry mode reads it, does not create it — Wave 1 has no staff-initiated table-open path yet) | **AP-3 Wave 1 (2026-08-27, corrected AP-3 Stage A report §1/§6).** The NEW, table-level dining-visit aggregate — deliberately a distinct collection from `tableGuestSessions` (one guest's own visit record) and `tableGuestSessions.tableSessionId` (a pre-existing, differently-scoped field name never reused for this concept — see that row's own note). Fields: `organizationId`/`restaurantId`/`branchId`/`tableId`, `status` (`'active'`\|`'closed'`\|`'cancelled'` — Wave 1 only ever writes `'active'`; close/cancel are not yet implemented), `openedAt`, `closedAt` (nullable), `openedByType` (`'guestQrScan'`\|`'staff'`), `openedByStaffUid` (nullable), `transferredFromTableId` (nullable, reserved for a future transfer callable — not yet implemented), `version`. **Corrected, AP-3 Wave 1 security correction (2026-08-27, ADR-037)**: NO client read path at all, for any actor including staff — a Security Rule cannot verify AP-2's trusted-device proof, so staff read exclusively through `getPosTableOperationalView` (`functions/src/posOperationalView.ts`, permission + device-session gated). Never client-writable, unchanged. |
| `guestSubAccounts/{subAccountId}` | deterministic `subaccount-{tableSessionId}-{uid}` for `guestSession`/`authenticatedCustomer`/`existingCustomer` owner types (keyed on the owning Firebase Auth uid alone — a single uid is structurally either a real phone customer or an anonymous guest, never both, so no ownerType-derived id prefix is needed), the deterministic singleton `subaccount-{tableSessionId}-staffGeneral` for the unattributed catch-all, or a generated id for a staff-created `namedWalkIn` | `organizationId`/`branchId` (own fields) | Cloud Function only — `openTableGuestSession` (creates the `guestSession`/`authenticatedCustomer` sub-account), `submitDineInOrder` (staffEntry mode creates `namedWalkIn`/`staffGeneral`/`existingCustomer`, and stamps a guest's mandatory `displayName` on first submission) | **AP-3 Wave 1, corrected Stage A report §1/§6/§8.** Fields: `organizationId`/`branchId`/`tableSessionId`, `ownerType` (`'guestSession'`\|`'authenticatedCustomer'`\|`'namedWalkIn'`\|`'staffGeneral'`), `ownerSessionRef` (nullable, audit/reference only — NEVER used in any rule or authorization decision), `ownerAuthUid` (nullable — the ONLY rules-authorization field, the caller's real Firebase Auth uid denormalized exactly like `tableGuestSessions.guestAuthUid`; `null` only for `staffGeneral`/`namedWalkIn`, which have no owning auth identity), `displayName` (mandatory before a guest's first order submission — enforced by `submitDineInOrder`, not by this collection's own rule), `status` (`'open'`\|`'closed'`\|`'merged'`\|`'voided'` — Wave 1 only ever writes `'open'`; the others are reserved for Wave 2's Check-lifecycle integration), `createdAt`, `createdByStaffUid` (nullable), `version`. **Corrected ownership model**: the original Stage A draft proposed keying an anonymous guest's sub-account off a `tableGuestSessions` document id — broken, since a Security Rule cannot compare a session doc id to `request.auth.uid`. `ownerAuthUid` is the fix, mirroring the already-established `guestAuthUid`/`orders.guestAuthUid` denormalization pattern. **Corrected, AP-3 Wave 1 security correction (2026-08-27, ADR-037)**: the owner's own read (`ownerAuthUid == request.auth.uid`) is unaffected — this is a customer/guest reading their own record, not a POS action. The staff branch is REMOVED — staff read exclusively through `getPosTableOperationalView`, same reasoning as `tableSessions` above. Never client-writable, unchanged. |
| `checks/{checkId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `checkOperations.ts` (`openCheck`/`cancelCheck`/`finalizeCheckReadyForPayment`/`reopenCheck`/five split callables/`mergeChecks`/`transferCheckAllocation`), `checkFinancialAdjustments.ts` (adjustment application/reversal decrement/restore `computedTotalMinorUnits`), **AP-4 Wave A (2026-08-31, ADR-044) — `paymentEngine.ts`'s `recordPaymentAttempt`, the only writer of `status: "paid"`** | **AP-3 Wave 2 (2026-08-27, ADR-037); status enum extended AP-4 Wave A.** Fields: `organizationId`/`branchId`/`tableSessionId`, `status` (`'open'`\|`'readyForPayment'`\|`'paid'`\|`'cancelled'` — `'paid'` is AP-4's own addition, written exactly once when a `PaymentSession`'s settled total reaches its payable amount), `paymentActivityStarted` (bool, AP-3 always writes `false` — AP-4's own first `PaymentAttempt` is the only thing that ever flips it), `computedTotalMinorUnits` (authoritative only because it is recomputed, transactionally, from this check's own active `checkAllocations` on every mutation — never independently editable), `currencyCode`, `openedAt`, `readyForPaymentAt`/`cancelledAt` (nullable), `createdByStaffUid`, `version`. No client read path at all — `getPosTableOperationalView` only. |
| `paymentIntents/{intentId}` | deterministic `intent-{checkId}-v{check.version}` | `organizationId`/`branchId` (own fields) | Cloud Function only — `paymentEngine.ts`'s `createPaymentIntent` | **AP-4 Wave A (2026-08-31, ADR-044).** A frozen, re-verifiable snapshot of what a `readyForPayment` check owes, grouped by `subAccountId` from real active `checkAllocations` (never independently re-derived) plus branch-authoritative cover/service charges (`branchPaymentConfig`). Fields: `organizationId`/`branchId`/`checkId`, `checkVersionAtCapture`, `subAccountAllocations` (`{subAccountId, payableAmountMinorUnits}[]`), `payableAmountMinorUnits`, `serviceCharges`, `currencyCode`, `createdAt`/`createdByStaffUid`, `supersededAt` (nullable). Total Firestore lockdown — no client read path at all yet; a purpose-built operational-view callable is a Wave D item. |
| `paymentSessions/{sessionId}` | deterministic `session-{checkId}` | `organizationId`/`branchId` (own fields) | Cloud Function only — `paymentEngine.ts` (`createPaymentIntent` creates/repoints, `recordPaymentAttempt` advances) | **AP-4 Wave A.** At most one non-terminal session per check. Fields: `organizationId`/`branchId`/`checkId`/`intentId`, `status` (`'collecting'`\|`'readyToComplete'`\|`'completing'`\|`'completed'`\|`'cancelled'`\|`'failed'` — explicit transition table, server-rejected if invalid, see `paymentDomain.ts`), `payableAmountMinorUnits` (frozen at intent capture), `settledAmountMinorUnits` (kept current, sum of settled attempts), `currencyCode`, `cashTipMinorUnits`/`cardTipMinorUnits`, `createdAt`/`createdByStaffUid`/`updatedAt`, `version`. Total lockdown, no client read path yet. |
| `paymentAttempts/{attemptId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `paymentEngine.ts`'s `recordPaymentAttempt` | **AP-4 Wave A.** One immutable-once-resolved record per tender action; cash/Boncuk resolve synchronously in one transaction, card/mealCard reserve at `providerPending` then resolve via a real external round-trip strictly outside any transaction (never holding a transaction open across a network call). Fields: `organizationId`/`branchId`/`checkId`/`sessionId`/`intentId`, `tenderType` (`'cash'`\|`'card'`\|`'mealCard'`\|`'boncuk'`), `status` (explicit transition table — `initiated`\|`providerPending`\|`succeeded`\|`declined`\|`timedOut`\|`unknownReconciliationRequired`\|`resolvedSucceeded`\|`resolvedFailed`\|`reversed`), `amountMinorUnits`, `currencyCode`, `allocations` (`{subAccountId, orderLineRefs, amountMinorUnits}[]`), `idempotencyKey` (a retry with the SAME key returns the original attempt, never a duplicate), `providerRef`/`providerResponseSummary` (never raw PAN/CVV/PIN/OTP), `loyaltyLedgerEntryId` (set only for `boncuk`), `declineReason`, `createdAt`/`createdByStaffUid`/`resolvedAt`, `correlationId`. Total lockdown, no client read path yet. |
| `refundRequests/{refundId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `paymentRefund.ts` (`requestPaymentRefund` creates `pendingApproval`; `applyPaymentRefund` — the allowlisted `paymentRefund` remote-approval handler — resolves per-allocation) | **AP-4 Wave A.** Full/partial refund, mixed-tender apportionment via `campaignPricing.ts`'s existing `allocateProportionally` largest-remainder helper (never a hand-rolled rounding scheme), each allocation refunded to its own original tender (cash settles immediately — pending Wave B's real `CashMovement`; Boncuk restores through the real loyalty ledger; card/mealCard use the same certification-only fallback `refundTakeawayOrder.ts` already established, honestly, since no real payment provider is configured). Fields: `organizationId`/`branchId`/`checkId`, `refundType` (`'full'`\|`'partial'`), `orderLineRefs` (nullable), `amountMinorUnits`, `allocations` (`{originalAttemptId, tenderType, amountMinorUnits, status, providerRef, cashMovementId, loyaltyLedgerEntryId}[]`), `status`, `reasonCode`/`reasonMessage`, `requestedByStaffUid`, `approvalRequestRef`, `createdAt`/`resolvedAt`, `version`. Total lockdown, no client read path yet. |
| `branchPaymentConfig/{branchId}` | the branch's own id | `organizationId`/`branchId` (own fields) | Not yet written by any callable this wave — read-only consumer (`createPaymentIntent`'s `computeServiceCharges`) exists; a branch-settings admin callable is a Wave D item | **AP-4 Wave A (contract only; no writer yet).** Server-resolved cover/service charge policy, cash register model, refund window, business-day timezone/cutover — never client-trusted. Fields: `organizationId`/`branchId`, `coverCharge`/`serviceCharge` (`{enabled, basis: 'flat'\|'percentage'\|'perPerson', amountMinorUnits, discountable}` or `null`), `cashRegisterModel` (`'sharedDrawer'`\|`'cashierBound'`), `showExpectedCashBeforeCount`, `refundWindowDays` (nullable), `businessDayCutoverHour`, `timezone`, `updatedAt`, `version`. Total lockdown, no client read path. |
| `checkAllocations/{allocationId}` | generated id | `organizationId`/`branchId`/`tableSessionId` (own fields) | Cloud Function only — same writers as `checks` above | **AP-3 Wave 2.** A FLAT top-level collection (preserves this codebase's established convention — no `checks/{id}/allocations` subcollection). Fields: `checkId`, `subAccountId`, `splitMethod` (`'product'`\|`'quantity'`\|`'customer'`\|`'headcount'`\|`'freeAmount'`), `sourceComposition` (array of `{sourceOrderId, sourceLineIndex, quantity: number\|null, amountMinorUnits}` — every allocation, regardless of split method, traces to real accepted order-line money; there is no unreferenced bare-amount shape), `allocatedAmountMinorUnits` (always `sum(sourceComposition[].amountMinorUnits)`), `currencyCode`, `status` (`'active'`\|`'reversed'`\|`'transferred'`), `note`, `createdAt`, `createdByStaffUid`, `version`. No client read path — `getPosTableOperationalView` only. |
| `orderLineAllocationLedgers/{sourceOrderId}_{sourceLineIndex}` | deterministic composite | `organizationId`/`branchId`/`tableSessionId` (own fields) | Cloud Function only — `checkOperations.ts`, written inside the SAME transaction as the `checkAllocations` document it backs | **AP-3 Wave 2.** The real concurrency lock proving conservation — never a query-returns-nothing assumption (mandatory Stage B refinement #3). Fields: `sourceOrderId`/`sourceLineIndex`, `originalQuantity`/`lineValueMinorUnits` (snapshotted once, at first allocation against this line — lines are immutable once accepted), `remainingQuantity`/`remainingValueMinorUnits` (decremented transactionally by every allocation touching this line, across every check in the table session), `currencyCode`, `version`. Never client-readable, not even via `getPosTableOperationalView`'s own response shape — internal accounting only. |
| `checkFinancialAdjustments/{adjustmentId}` | generated id | `organizationId`/`branchId` (own fields) | Cloud Function only — `checkFinancialAdjustments.ts` (`requestCheckFinancialAdjustment` creates `pendingApproval`; `applyCheckFinancialAdjustment` — the allowlisted `checkFinancialAdjustment` remote-approval handler — sets `active`; `reverseCheckFinancialAdjustment` sets the original `reversed` and creates a NEW compensating record) | **AP-3 Wave 2.** Structurally separate from the Boncuk ledger (`loyaltyLedgerEntries`/`loyaltyAccounts`) — no shared code path with `boncukBalanceCorrectionRequests` below. Fields: `checkId`, `scope` (`'product'`\|`'subAccount'`\|`'check'`), `scopeRef` (`{allocationId, subAccountId}`), `adjustmentType` (`'complimentary'`\|`'percentage'`\|`'fixedAmount'`), `percentageBasisPoints`/`fixedAmountMinorUnits` (nullable, per type), `requestedAppliedAmountMinorUnits` (request-time snapshot, display/audit only), `appliedAmountMinorUnits` (nullable until approved — the REAL applied amount, always recomputed fresh against live state inside the approval handler, never trusted from the snapshot), `currencyCode`, `reasonCode`/`reasonMessage`, `approvalRequestRef`, `requestedByStaffUid`, `status` (`'pendingApproval'`\|`'active'`\|`'rejected'`\|`'reversed'`), `reversalOf` (nullable), `calculationSnapshot`, `createdAt`, `version`. Never client-readable. |
| `acceptedLineCancellationRequests/{orderId}_{lineIndex}_{epochMs}` | composite | `organizationId`/`branchId` (own fields) | Cloud Function only — `checkFinancialAdjustments.ts` (`requestAcceptedLineCancellation`/`applyAcceptedLineCancellation`, the allowlisted `acceptedLineCancellation` remote-approval handler) | **AP-3 Wave 2.** Only an `accepted` line is eligible. On approval, the referenced `orders.lines[i].status` transitions to the distinct terminal value `'cancelledAfterAcceptance'` (never reusing `'rejected'`) and gains a `cancellation` object (`originalStatus`/`requestedByStaffUid`/`approvedByStaffUid`/`reasonCode`/`reasonMessage`/`approvalRequestRef`/`cancelledAt`). Never client-readable — the requester's own status visibility comes from the already-real `remoteApprovalRequests` read rule. |
| `orderLineCancellationEvents/{orderId}_{lineIndex}` | deterministic composite | `organizationId`/`branchId` (own fields) | Cloud Function only — written transactionally alongside the line-status transition above, `.set()` not `.create()` (idempotent — a duplicate handler invocation for the same approval can never double-emit) | **AP-3 Wave 2.** Outbox marker for AP-5's own future stock/waste/fire reconciliation consumer — NOT built or implied by this row. Fields: `orderId`/`lineIndex`/`organizationId`/`branchId`, `preparationStarted` (best-effort boolean derived from the order's own `status` at cancellation time — `'preparing'` or later — an honest, disclosed proxy; AP-5 owns building a real per-line KDS "fired" signal), `approvalRequestRef`, `recordedAt`, `consumedByStockReconciliation` (always `false` — no consumer exists yet). Never client-readable. |
| `boncukBalanceCorrectionRequests/{organizationId}_{customerId}_{epochMs}` | composite | `organizationId` (own field) | Cloud Function only — `checkFinancialAdjustments.ts` (`requestBoncukBalanceCorrection`/`applyBoncukBalanceCorrection`, the allowlisted `boncukBalanceCorrection` remote-approval handler) | **AP-3 Wave 2.** The ONLY code path permitted to write a loyalty `adminAdjustment` ledger entry (`loyaltyLedgerEntries`/`loyaltyAccounts` — pre-existing collections, this is their first real `adminAdjustment` writer). Fields: `organizationId`/`customerId`/`deltaBoncuk`/`reasonCode`/`reasonMessage`/`requestedByStaffUid`/`approvalRequestRef`, `status` (`'pendingApproval'`\|`'applied'`\|`'rejected'`). Approval-request `branchId` is the literal string `"platform"` (a disclosed simplification — Boncuk balance correction is an organization-level, not branch-scoped, financial-exception action; an approver's own branch-scoped Approval Inbox query may not surface it — no UI consumes this yet, so this is a backend-only, not yet user-facing, limitation). Never client-readable. |
| `platformCustomerDirectoryEntries/{uid}` | the customer's own uid | — (platform-owned, no tenant field) | Cloud Function only — `completeCustomerProfile.ts` (populated immediately on real profile completion, never inventing an org relationship) | **AP-3 continuation (2026-08-27, ADR-038).** Exactly one entry per registered customer, including one with no tenant relationship at all — the sole correct source for "every registered customer" (`customerDirectoryEntries` below cannot answer this; it duplicates per-tenant and excludes no-tenant customers). Fields: `uid`, `displayName`, `displayNameNormalized`, `phoneNumber` (internal only, never returned by any read API), `phoneSearchHash` (HMAC-SHA256, `defineSecret("CUSTOMER_PHONE_SEARCH_HMAC_SECRET")` — never an unkeyed duplicate), `registrationDate`, `accountState`, `updatedAt`, `version`. No direct client read path — `listPlatformCustomers`/`searchPlatformCustomersByPhone`/`getPlatformCustomerDetail` (Platform-capability-gated) only. |
| `customerDirectoryEntries/{organizationId}_{uid}` | composite | `organizationId` | Cloud Function only — `completeCustomerProfile.ts` (created only from the real membership write, `!membershipAlreadyExists`), `onOrderCreatedForCustomerDirectory` (order-driven incremental maintenance: `lastActivityAt`/`lastOrderAt`/`totalOrderCount`/`relatedBranchIds`) | **AP-3 continuation.** Tenant-scoped relationship projection — created only from a verified tenant interaction, never speculatively. Fields mirror the platform entry plus `lastActivityAt`, `relatedBranchIds`, `lastOrderAt`, `totalOrderCount`. No direct client read path — `listTenantCustomers`/`searchCustomersForPos`/`getTenantCustomerDetail` (`viewTenantCustomerDirectory` staff permission) only. Address visibility for tenant/branch staff is limited to that org's own `orders.deliveryAddressSnapshot` history — this projection never stores or exposes `customerAddresses` directly. Backfill for customers who registered before this backend existed is explicitly deferred (disclosed) — no real production data exists yet to backfill against. |
| `tenantCustomerRestrictions/{organizationId}_{uid}` | composite | `organizationId` | Cloud Function only — `setTenantCustomerRestriction` (`manageTenantCustomerRestriction`, manager+-tier) | **AP-3 continuation.** Tenant-scoped only — proven (direct test) to never touch the canonical global `customers/{uid}` document. Fields: `organizationId`, `customerId`, `status` (`'active'`\|`'none'`), `reasonCode`, `reasonMessage`, `expiresAt` (nullable), `actorUid`, `createdAt`, `version`. No direct client read path. |
| `platformCustomerRestrictions/{uid}` | the customer's own uid | — (platform-owned) | Cloud Function only — `setPlatformCustomerRestriction` (`customerDirectory.globalRestriction` capability, Platform Owner only) | **AP-3 continuation.** Separate collection AND separate capability from the tenant restriction above — deliberately never conflated. No direct client read path. |
| `memberships/{organizationId}_{uid}` | composite | `organizationId` | Cloud Function only | Source of truth for custom claims — `roles: [String]`, `branchAccess/restaurantAccess/organizationAccess: [String]`, `status`. Client: read own membership docs only, never another user's. |
| `staffMembers/{staffMemberId}` | sequential id | `organizationId` | Cloud Function only | Mirrors `StaffMember`. Kept distinct from `memberships` — `memberships` is the claims-sync source of truth (minimal shape); `staffMembers` is the full admin-facing record (display name, session-revocation timestamp, etc.). |
| `customers/{uid}` | **Firebase Auth UID** | — (global identity, Sprint 9C) | Cloud Function + the owning user (profile fields only, never role/status fields) | The canonical global identity `AuthSession`/`ProfileModel`/CRM `Customer` are intended to converge onto (Sprint 9C's own framing; `docs/phase9_architecture_analysis.md` §7 — still not fully done, see the CR.1 note below). **Customer Registration CR.1 (2026-08-19)**: the first real server-side writer, `completeCustomerProfile` (Admin SDK, `functions/src/completeCustomerProfile.ts`) — closes the gap where no Cloud Function ever created this document at all (only `processAccountDeletion` updated a pre-existing one). Fields as of CR.1: `uid`, `firstName`, `lastName`, `displayName` (server-derived from `firstName`+`lastName`, never client-supplied directly), `email`, `phoneNumber` (from the verified auth token, never client-supplied), `accountStatus` (`'active'`\|`'restricted'`, `CustomerAccountStatus`'s existing canonical values — no second status model introduced), `occupationStatus` (`'working'`\|`'student'`\|`'other'`), `workplaceName`/`educationalInstitutionName` (mutually exclusive per `occupationStatus`, both `null` for `'other'`), `gender` (`'female'`\|`'male'`\|`'preferNotToSay'`), `profileCompletedAt`, `createdAt`, `updatedAt`. **Personalization data only — `occupationStatus`/`workplaceName`/`educationalInstitutionName`/`gender` are never authorization inputs**, never referenced by any Firestore rule or Cloud Function permission check. The client `update` allow-list (`firestore.rules`) intentionally still covers only `displayName`/`email` — the new CR.1 fields are set once at registration by the Cloud Function and are not client-editable afterward (no profile-edit flow exists yet). **CRM `Customer` (`lib/features/crm/`) remains `InMemory*`, deliberately out of CR.1's scope** — this document is the sole persisted identity; the CRM/`AuthSession`/`ProfileModel` three-way convergence Sprint 9C originally framed is still open future work, not something CR.1 claims to finish. **New field, Customer Registration CR.1.1 (2026-08-20)**: `birthDate` — a plain `"YYYY-MM-DD"` string (date-only, never a `Timestamp`, never a locale-formatted string like `"20/08/1990"` — avoids inventing a time-of-day/timezone that has no meaning for a calendar date). Required, and immutable after its first write: `completeCustomerProfile` only ever merges `birthDate` into its write patch when the existing document doesn't already carry a valid one, so a later repair call (triggered by some other missing field) can never overwrite an already-set value; the client `update` allow-list never includes it either, so `completeCustomerProfile` (Admin SDK) is the only writer, ever. PII — redacted (`null`) by `processAccountDeletion` on account deletion, same as `gender`/`occupationStatus`. A future customer-requested correction is intended to go through a separate, not-yet-built admin-approval flow (documented in `docs/decisions.md`'s CR.1.1 entry), never a direct write to this field. **New client reader, P.4.3B (2026-08-20)**: `CustomerIdentityGateway`/`customerIdentityProvider` (`lib/features/profile/`) now read this document directly (`.snapshots()`, owner-only per this rule — no rule change) to render Profile's real hero identity (`firstName`/`lastName`/`email`/`occupationStatus`/`workplaceName`/`educationalInstitutionName`) — the first client consumer of this document; `ProfileModel`/`profileProvider` remain a separate, unmodified in-memory bridge. |
| `tenantCustomers/{organizationId}_{uid}` | composite | `organizationId` | Cloud Function only (loyalty/visit data is server-computed) | Per-tenant CRM profile — visit counts, reward history (not yet implemented — see below). References `customers/{uid}`. **Customer Registration CR.1**: `completeCustomerProfile` is now also the first real writer of this collection (previously nothing created it in production, only test fixtures) — one document per `(organizationId, uid)`, created exactly once during "Profilini Tamamla" and left untouched afterward (repaired, never overwritten, on a repeat bootstrap call). Fields as of CR.1: `organizationId`, `uid`, `createdAt` — the `visit counts, reward history` shape this row already described remains a documented future addition, not yet written by any code. |
| `entitlements/{entitlementId}` | sequential id | `organizationId` | Cloud Function only | Mirrors `EntitlementGrant`. Client: read only. **Clients cannot write arbitrary entitlement grants** — this is the literal, direct enforcement of that named requirement. |
| `orders/{orderId}` | canonical `OrderId` (Sprint 9D) — for a takeaway order created via `submitTakeawayOrder` (Faz D.3), a deterministic `sha256(actorUid\|submissionKey)` derived server-side, never client-chosen | `organizationId`, `branchId` | Client create (staff/dine-in, remaining direct-write paths) with server-validated shape **or** Admin SDK via `submitTakeawayOrder` — **Faz D.3.1**: `submitTakeawayOrder` is now the sole create path for **both** takeaway scenarios (QR guest and authenticated app customer); `firestore.rules`'s old authenticated-takeaway direct-create branch (`isValidAuthenticatedTakeawayOrder`) has been removed. Staff/dine-in QR (guest+authenticated)/delivery/reservation direct-create paths are unaffected. Status transitions Cloud-Function-only after creation | The one canonical `Order` aggregate (Sprint 9D) — no second, competing order collection. `submitTakeawayOrder` writes the exact same document shape as the client-side `OrderFirestoreMapper.toFirestore` path, including the additive `takeawaySubmissionFingerprint` field (idempotency, Faz D.3, never read by `OrderFirestoreMapper.fromFirestore`). **New field, Faz R.1C.2**: `reservationContextId` (nullable) — mirrors the originating `tableGuestSessions.reservationContextId` 1:1; for the two dine-in-QR create branches, `firestore.rules`' `tableGuestSessionMatchesOrderScope` requires exact equality between the two (missing-field-safe `.get(key, null)` on both sides, `null` included) — a client can neither omit it, coerce it to `null`, nor claim a different reservation's context. Not an identity field: never produces `customerId`, never inherits a reservation owner's uid, never creates loyalty/CRM ownership. **Faz R.1D.1 — second, distinct meaning reusing the same field**: for the new `reservationPreorder` channel, `reservationContextId == Reservation.id` directly (this order *is* the customer's own preorder for that reservation, `customerId` is their real phone-auth uid) — disambiguated from the Faz R.1C.2 dine-in-QR meaning purely by `channel`, never a second field. A `reservationPreorder` order is created exclusively via `submitReservation`'s Admin SDK transaction — `firestore.rules`' `orders` `create` rule has no branch matching this channel at all, mirroring the already-closed `takeaway` precedent. **New field, Faz R.1D.1**: `kitchenReleaseAt` (nullable ISO string) — only ever set/meaningful for a `reservationPreorder` order; `null` until the linked reservation confirms, then `confirmedTime - PREORDER_KITCHEN_RELEASE_LEAD_MINUTES` (locked at 60). Cloud-Functions-only, additive — no Dart `Order` model field was added for it this phase (no UI consumes it yet; harmless extra key on read). **New field, Faz R.1D.2**: `kitchenReleaseAtTimestamp` (nullable Firestore `Timestamp`) — the canonical companion of `kitchenReleaseAt`, always set/cleared together with it (never independently); mirrors `pickupTime`/`pickupTimeTimestamp`'s own established dual-representation precedent (ISO string for display/back-compat, a real `Timestamp` for range/orderBy queries). This is the field `reservationPreorderKdsRelease`'s candidate query actually filters/orders on — see the composite index below. **New writer, Faz R.1D.2**: the scheduled function `reservationPreorderKdsRelease` (`onSchedule("every 1 minutes")`, `src/reservationSweep.ts`) transitions a due order `pendingConfirmation -> confirmed`, inside a transaction that independently re-verifies every invariant (Order/Reservation linkage both directions, `Reservation.status == 'confirmed'`, and an exact recomputed-vs-stored `kitchenReleaseAtTimestamp` match) rather than trusting the candidate query alone — a mismatch is a data-integrity condition, never silently released or repaired. Requires the new composite index `orders(channel ASC, status ASC, kitchenReleaseAtTimestamp ASC)`. **New reader, Faz R.3C**: `FirestoreKitchenTicketRepository` (`lib/features/pos/data/`) is the production KDS board's sole read source — `orders.where('branchId','==',branchId).where('status', whereIn: ['confirmed','preparing','ready'])`, client-side sorted by `firedAt`, live via `.snapshots()`. Requires the new composite index `orders(branchId ASC, status ASC)`. No new writer, no new field — this is a pure additional read pattern over the existing document shape, identical for every channel. **New fields, Faz P.1**: `deliveryAddressSnapshot`/`paymentMethodSnapshot` (both nullable maps, additive — a missing key reads as `null`, same backward-compatibility contract every other additive field on this document already has). Neither is ever written by any production code path this phase — no `submitDeliveryOrder` callable exists yet, and `deliveryAddressSnapshot` specifically requires a real, server-verified address (`serverVerifiedAt`) that no provider can produce until Faz P.2. Documented here as reserved shape only. **New field, Boncuk Loyalty P2A security fix (2026-08-21)**: `pricingAuthority` (nullable string, canonical constant `"serverV1"` — `functions/src/orderPricingAuthority.ts`). **`channel` and `pricingAuthority` are deliberately NOT interchangeable concepts, and no earlier P2A documentation should be read as claiming otherwise**: `channel` identifies the commercial/order flow only — a client create can set it to any value the `isOrgMember`/table-guest `firestore.rules` branches allow (confirmed: a staff/POS actor can create a direct-Firestore `channel: 'takeaway'` or `channel: 'reservationPreorder'` order today, a legitimate pre-existing capability unrelated to loyalty) — and says nothing about how `pricing` was produced. `pricingAuthority` identifies, independently, whether `pricing` was produced by an approved trusted SERVER pricing pipeline; it is stamped only by `submitTakeawayOrder`/`submitDeliveryOrder`/`reservationPreorder` (all Admin SDK, bypass `firestore.rules`), never accepted from a request parameter, and `firestore.rules`' `orders` `create` rule (`clientOrderCreateOmitsPricingAuthority()`) denies any client create — on any channel, via any of the three create branches — that supplies this field at all. `loyaltyOrderEarning.ts` requires both an eligible `channel` AND a valid `pricingAuthority` before earning; channel alone is never sufficient. `null`/absent on every pre-2026-08-21 order (no migration back-stamps old orders as trusted — a legacy order permanently does not earn under this check, which is the correct, intended outcome, not a gap to fix). **New fields, Boncuk Loyalty P4-B (2026-08-22) — checkout redemption, takeaway only**: `selectedBenefitType` (closed string `'none'`\|`'boncukRedemption'`\|`'coupon'`\|`'catalogReward'` — only the first two are reachable this phase; `firestore.rules`' `clientOrderCreateOmitsBoncukRedemption()` denies any client create claiming `'boncukRedemption'`) and `boncukRedemption` (nullable map, non-null only when `selectedBenefitType == 'boncukRedemption'`: `boncukUsed`, `valueMinorUnits`, `remainingPayableMinorUnits`, `redemptionValueMinorUnitsPerBoncuk`, `maxRedemptionBasisPoints`, `loyaltyPolicyVersion` — a server-written settlement snapshot, never a `pricing` mutation; `pricing.discount`/`pricing.grandTotal` are untouched by redemption, see `BR-LOYALTY-019`). Written only by `submitTakeawayOrder.ts`'s authenticated (non-guest) path; the guest path always writes `selectedBenefitType: 'none', boncukRedemption: null`. **Boncuk Loyalty P5-B (2026-08-24) — second real writer, delivery.** `submitDeliveryOrder.ts`'s transaction now writes the IDENTICAL schema (same field names/shapes, same `clientOrderCreateOmitsBoncukRedemption()` rules-level guard, reused verbatim, not a second variant) — the one difference is the eligible-basis computation, `pricing.grandTotalMinorUnits` directly (delivery has no separate delivery-fee line to subtract, unlike takeaway's tip-subtraction step). `reservationPreorder`/dine-in/POS orders still never carry either field (redemption not wired for those channels this phase; dine-in/POS excluded per the same `BR-PRICE-002` server-pricing-authority gap `pricingAuthority` above already documents). **Boncuk Loyalty P5-B (2026-08-24) — canonical delivery lifecycle, `BR-LOYALTY-024`.** Five new Cloud Functions are now, for the first time, real post-creation writers of a delivery order's `status`, mirroring the takeaway lifecycle callables' shape exactly: `respondToDeliveryOrder` (`pendingConfirmation -> confirmed`\|`rejected`), `advanceDeliveryOrderStatus` (`confirmed -> preparing -> ready -> outForDelivery -> completed`, one exact step at a time — one extra step versus takeaway, since delivery has no `served` concept but does have `outForDelivery`), `cancelDeliveryOrder` (`pendingConfirmation -> cancelled`, the owning customer only), `cancelDeliveryOrderForStaff` (`confirmed -> cancelled` baseline; `preparing`\|`ready`\|`outForDelivery -> cancelled` escalated-permission tier), `refundDeliveryOrder` (`completed -> refunded`, manager+ only). The underlying write-phase helpers (`applyOrderLifecycleTransition`/`writeOrderStatusChangeAuditEvent`) were extracted from `takeawayOrderLifecycle.ts` into a neutral `orderLifecycle.ts` module and are now genuinely shared, not duplicated — `outForDelivery`, like `rejected`/`refunded`, has no dedicated `timestamps.*` slot (its own `statusHistory` entry is its only timestamp record, a pre-existing data-model gap, not new to this phase). `courier` holds none of the new lifecycle permissions — courier-authoritative delivery completion remains deferred. **Boncuk Loyalty P4-C-C-B (2026-08-22) — the canonical takeaway lifecycle, `BR-LOYALTY-021`.** Four new Cloud Functions are now, for the first time, real post-creation writers of a takeaway order's `status`: `respondToTakeawayOrder` (`pendingConfirmation -> confirmed`\|`rejected`, staff-only), `advanceTakeawayOrderStatus` (`confirmed -> preparing -> ready -> completed`, one exact step at a time, staff-only), `cancelTakeawayOrder` (`pendingConfirmation -> cancelled`, the owning customer only), `cancelTakeawayOrderForStaff` (`confirmed`\|`preparing`\|`ready -> cancelled`, staff/manager-tier depending on kitchen commitment already spent). Each populates the existing, previously-always-`null` matching `timestamps.*` slot (`confirmed`/`preparing`/`ready`/`completed`/`cancelled` — no new duplicate timestamp concept added) and appends exactly one `statusHistory` entry per real transition (never on a duplicate/idempotent retry). **New fields, terminal transitions only (`rejected`\|`cancelled`)**: `terminalReasonCode` (closed enum, channel-appropriate — rejection vs. cancellation each have their own closed set, `functions/src/takeawayOrderLifecycle.ts`), `terminalActorType` (`'customer'`\|`'staff'`\|`'system'`), `terminalAt` (ISO string). **Deliberately, by design, `terminalActorUid` is never written to this document at all** — a staff member's uid is internal operational data; it exists only in the matching `auditEvents` record (see that row above). `firestore.rules`' `orders` `allow update: if false` is completely unchanged — all four callables are Admin SDK, bypassing rules entirely, exactly like every prior order-status writer. **Boncuk Loyalty P4-D-B (2026-08-22) — the fifth lifecycle callable, `refundTakeawayOrder` (`completed -> refunded`, manager+ only via the new `manageTakeawayOrderRefunds` permission), and a new field.** `order.status == "refunded"` is LOCKED to mean the refund has ACTUALLY been completed/confirmed, never merely requested/authorized/pending/failed — this callable CERTIFIES a real, complete monetary refund already happened through some business-side process outside this system's own visibility (cash/manual terminal/external process), it never moves money itself. New field: `refundDisposition` (closed string, `TAKEAWAY_REFUND_DISPOSITIONS` — `'manualExternalRefundConfirmed'` is the only value any writer can produce this phase; `'providerRefundSucceeded'` is reserved and structurally unreachable until a real payment-provider refund executor exists — there is deliberately no pending/failed value, since a provider's pending or failed refund attempt must never set `status: 'refunded'`), written only for this transition. Reuses the existing `terminalReasonCode`/`terminalActorType`/`terminalAt` fields with a THIRD closed reason-code enum (`TAKEAWAY_REFUND_REASON_CODES`, deliberately separate from rejection's and cancellation's own) — `terminalActorUid`/internal `reasonMessage` remain `auditEvents`-only, unchanged discipline. **AP-3 Wave 1 (2026-08-27) — `submitDineInOrder` extended with a discriminated `mode` (`'guestSession'`\|`'staffEntry'`, corrected AP-3 Stage A report §10; default `'guestSession'` when omitted, preserving every pre-Wave-1 caller).** New top-level fields: `mode`, `subAccountId` (resolves to `guestSubAccounts/{subAccountId}`), `dineInSessionGroupId` (the NEW table-level `tableSessions/{id}` this order belongs to — deliberately a distinct field/concept from the pre-existing `tableSessionId`, which still means a `tableGuestSessions` document id and is `null` for a `staffEntry` order), `linesDispositionSummary` (`'pending'`\|`'partiallyResolved'`\|`'resolved'`). Each `lines[i]` gains `subAccountId` and `status` (`'pendingApproval'`\|`'accepted'`\|`'rejected'`, plus a reserved `counterProposal` field, always `null` this phase — proposal/counter-proposal itself is not yet implemented). `staffEntry`-mode lines are written `status: 'accepted'` immediately (a trusted, `manageDineInOrders`-permission- and `requireActiveDeviceSession`-gated staff entry IS the approval — never routed through `respondToDineInOrderLines`, structurally preventing a self-approval loop). New callable `respondToDineInOrderLines` (`functions/src/respondToDineInOrderLines.ts`) is the sole writer of a `guestSession`-mode line's `accepted`/`rejected` transition post-creation. |
| `orderEvents/{eventId}` | generated id | `organizationId` | Cloud Function only | The event/outbox records (Sprint 9F) — idempotency key, correlation id, status, attempt count, next retry time, failure reason (redacted, no PII/secrets). Two writers exist today: `onOrderCompleted.ts` (`{orderId}-completed`, `type: 'order.completed'`, consumed by `loyaltyOrderEarning.ts`). **New writer, Boncuk Loyalty P4-C-B (2026-08-22)**: `onOrderTerminalFailureOrRefund.ts` — a generic, passive `onDocumentUpdated` trigger mirroring `onOrderCompleted.ts`'s exact shape, writing `{orderId}-{terminalStatus}` (`type: 'order.rejected'` \| `'order.cancelled'` \| `'order.refunded'`) whenever an order transitions INTO one of those three statuses, from any prior status. Same fields as `order.completed`'s event, plus `boncukRedemptionRestoreEvaluated: false` (flipped only by `loyaltyRedemptionRestore.ts`'s own consumer, on a genuine success or deterministic no-op — never on an anomaly) and, `refunded` only, `earnReversalEvaluated: false` (an honest placeholder — no consumer for it exists yet, see `BR-LOYALTY-020`). Deliberately from-state-agnostic and decoupled from whichever future canonical transition writes the terminal status — reachable today for `reservationPreorder`'s existing `cancelReservation`/`markReservationNoShow`/`reservationSweep`-driven cancellations (a no-op in practice, since that channel cannot redeem Boncuk), and, as of Boncuk Loyalty P4-C-C-B/P4-D-B (2026-08-22), genuinely reachable in production for real `takeaway` orders via the canonical lifecycle callables (`cancelTakeawayOrder`/`cancelTakeawayOrderForStaff`/`respondToTakeawayOrder`'s reject decision, and now `refundTakeawayOrder` for `order.refunded`) — and, as of Boncuk Loyalty P5-B (2026-08-24), identically reachable for real `delivery` orders via the mirrored callables (`cancelDeliveryOrder`/`cancelDeliveryOrderForStaff`/`respondToDeliveryOrder`'s reject decision/`refundDeliveryOrder`), with zero changes to this trigger itself. **New consumer, Boncuk Loyalty P4-D-B (2026-08-22)**: `orderEarnReversal.ts`'s `onOrderEventCreatedForOrderEarnReversal` — a second, genuinely independent consumer of the SAME `order.refunded` record `loyaltyRedemptionRestore.ts` already consumes (neither knows about the other) — flips `earnReversalEvaluated` to `true` on a genuine success or a deterministic no-op (no original `orderEarn` entry to reverse), never on an anomaly. This closes the previously-honest "still owed" placeholder this field used to describe. |
| `auditEvents/{auditEventId}` | generated id | `organizationId` | Cloud Function only — `onOrderCreated.ts` (`created -> pendingConfirmation`); **Faz R.1D.2**: `buildPreorderConfirmationPatch`'s confirmed branch (`respondToReservation.ts`/`respondToProposedChange.ts`, immediate release) and `reservationPreorderKdsRelease`'s scheduled release both write a `type: 'order.statusChanged'` record here for a preorder's `pendingConfirmation -> confirmed` transition, atomically with that same write — the one existing generic order-transition audit mechanism, reused rather than duplicated into a new pattern. **Boncuk Loyalty P4-C-C-B (2026-08-22)** — `respondToTakeawayOrder`/`advanceTakeawayOrderStatus`/`cancelTakeawayOrder`/`cancelTakeawayOrderForStaff` (`functions/src/takeawayOrderLifecycle.ts`'s shared `writeTakeawayOrderStatusChangeAuditEvent`) extend this SAME `type: 'order.statusChanged'` shape with additive fields; **Boncuk Loyalty P5-B (2026-08-24)** — the 5 delivery lifecycle callables write the identical shape via the same underlying helper, now relocated to the neutral `functions/src/orderLifecycle.ts` (`writeOrderStatusChangeAuditEvent`, re-exported under its original name for takeaway) — `branchId`, `actorType` (`'customer'`\|`'staff'`\|`'system'`), `actorUid`, `actorRoles` (the caller's full role array for the org), `reasonCode`, `reasonMessage` (INTERNAL staff note — never written to the customer-readable order document itself, see the `orders` row below) — never a new parallel audit collection. Deterministic id (`${orderId}-status-${fromStatus}-${toStatus}`) — a given order can only make one specific transition once (the state machine has no cycles). | Append-only — no rule permits `update` or `delete` on this collection at all, only `create` (server-side) and `read` (member). |
| `deletionRequests/{requestId}` | generated id | — (keyed by `uid`, may reference multiple `organizationId`s the account has tenant data in) | Cloud Function only for status transitions; client may `create` a request for their own `uid` | Sprint 9G. |
| `deviceTokens/{tokenId}` | client-generated (`SequentialDeviceTokenIdGenerator`) | `organizationId` (branding scope) + `uid` | The owning user (their own tokens only); `create`/`update` require `request.resource.data.uid == request.auth.uid` | FCM tokens (Sprint 9H); **Faz R.3C**: `FirestoreDeviceTokenRepository` is now the real, production-backing implementation (previously real-but-entirely-disconnected scaffolding — no Firestore-backed repository, no real FCM token ever registered). Fields, matching the actual Dart `DeviceToken` model exactly (correcting this row's prior Sprint-9H prose, which had drifted to `platform`/`createdAt`/`updatedAt`/`active` — the real code has always used the names below): `uid`, `organizationId`, `token` (raw FCM registration token — never logged, `LogRedactor`-equivalent discipline), `platform` (`'android'`\|`'ios'`\|`'web'`, free-form string), `registeredAt`, `revokedAt` (nullable; `isActive` is computed as `revokedAt == null`, not a stored field). Re-registering the same physical token under a *different* uid (Faz R.3C bug fix) revokes the old owner's record and creates a fresh one — never left active under two uids at once. |
| `reservationNotificationDeliveries/{eventId}` | same id as the `reservationEvents` document it delivers (deterministic, so `.create()` on this doc is the exactly-once delivery claim) | — (single-tenant; not queried across organizations) | Cloud Function only — written by `onReservationEventCreated` (`reservationNotificationDelivery.ts`) | **Faz R.3C.** Delivery-state bookkeeping for reservation push notifications, kept separate from the immutable `reservationEvents` audit record itself (that collection's own shape/semantics are unchanged). Fields: `eventId`, `type`, `reservationId`, `status` (`'processing'`\|`'sent'`\|`'skipped'`), `reason` (set only when skipped — `'reservation-not-found'`\|`'no-customer'`\|`'no-active-tokens'`), `tokenCount`/`successCount` (set on `'sent'`), `createdAt`/`updatedAt`. Not client-readable or client-writable at all — no rule exists for this collection, so it falls under the default-deny match, the same structural "fail closed on anything unlisted" property `reservationEvents` itself already relies on. |
| `mediaMetadata/{mediaId}` | generated id | `organizationId` | Cloud Function only (validates the actual upload before the ref becomes trusted) | Opaque ref metadata only — mirrors `CustomerPhoto.photoRef`/`BrandAssetSet`'s existing "never raw bytes" domain-layer discipline, extended to the backend. |
| `customerAddresses/{addressId}` | generated id | — (owned by `uid`, not organization-scoped — an address is a customer identity concern, not tenant data) | `create`: Cloud Function only (`saveDeliveryAddress`, `functions/src/deliveryPlaces.ts`, Faz P.2) — independently re-resolves the given Google `placeId` server-side on every save, never trusting client-supplied address data. `update`: direct client SDK, allow-listed to non-authoritative metadata only (`label`/`isDefault`/`apartmentNo`/`floor`/`addressDescription`/`buildingNoOverride`/`updatedAt`) via `request.resource.data.diff(resource.data).affectedKeys().hasOnly([...])`, mirroring `customers/{uid}`'s own pattern. `delete`: direct client SDK, owner-only. | Firestore-canonical representation of the Dart `SavedAddress` domain model (`lib/features/orders/domain/models/saved_address.dart`). Fields: `uid`, `label`, `isDefault`, `apartmentNo` (always customer-entered), `floor`/`addressDescription`/`buildingNoOverride` (nullable, customer-entered), `verificationStatus` (`'unverified'`\|`'verified'`\|`'stale'`), `verifiedAt` (nullable), `providerSource` (`'google_places'`), `providerPlaceId`, `provinceName`/`districtName`/`neighborhoodName`/`streetName` (all nullable — a real coverage spike found even a legitimate place's address components are not always fully populated), `buildingNo`/`buildingNoSource` (`'provider'`\|`'customer'`, provenance preserved per Faz P.2 §6), `latitude`/`longitude`, `formattedAddress`, `createdAt`/`updatedAt`. `isDefault` is mutually exclusive across one customer's addresses, enforced transactionally inside `saveDeliveryAddress`. Not yet consumed by any real delivery order — `Order.deliveryAddressSnapshot` still requires a verified `SavedAddress` to be converted via `toDeliveryAddressSnapshot()`, and no `submitDeliveryOrder` callable exists yet (deferred to a future phase). |

| `customerPhotos/{photoId}` | generated id | `organizationId` (own field) | Cloud Function only — `finalizeCustomerPhotoUpload` (create, on Storage upload-complete trigger), `moderateCustomerPhoto`/`selectCustomerProfilePhoto` (status/selection transitions) | P.4.2A/P.4.3A. Fields: `customerId`, `organizationId`, `photoRef` (Storage object path, opaque), `status` (`'pendingReview'`\|`'underReview'`\|`'approved'`\|`'rejected'`\|`'removed'`), `isSelectedAsProfilePhoto`, `uploadedAt`, `rejectionReason` (nullable), `revision`. **New field, Customer Registration CR.1.2 (2026-08-20)**: `purpose` (nullable, closed enum — today only `'profileOnboarding'` or `null`) — server-authoritative, copied verbatim from the originating `customerPhotoUploadGrants` document by `finalizeCustomerPhotoUpload`, never client-supplied at this document's own write boundary. Read: owner (`customerId`) at **any** status, or same-org staff — never gated by status, matching the locked "owner sees their own pending/rejected/removed photo" requirement; a non-owner, non-staff caller has no read path to this collection at all (public visibility goes through `customerPublicProfiles` below instead, never this collection directly). Write: `allow write: if false` — no client write path exists, full stop. |
| `customerPublicProfiles/{organizationId}_{uid}` | composite | `organizationId` (own field) | Cloud Function only — `selectCustomerProfilePhoto` (manual selection); **Customer Registration CR.1.2**: `moderateCustomerPhoto`'s new auto-selection branch (see below) | P.4.1. The one minimal, explicitly public-safe projection of a customer's identity within one tenant — `uid`, `organizationId`, `selectedProfilePhotoRef` (nullable — the sole pointer `storage.rules`' public-read exception is gated on; never gated on `CustomerPhoto.status` alone), `updatedAt`. Read: same-org staff or same-tenant authenticated customer — never a guest, never cross-tenant. Write: Cloud-Function-only. **Auto-selection, CR.1.2**: when an authorized moderator approves a `customerPhotos` document whose `purpose == 'profileOnboarding'`, `moderateCustomerPhoto` checks this projection **inside the same transaction** — if `selectedProfilePhotoRef` is absent (no prior selection), it sets it to the newly-approved photo's `photoRef` and marks that photo `isSelectedAsProfilePhoto: true`; if a selection already exists, it is never overwritten. A matching `auditEvents/{photoId}-auto-selected` record (`actorId: 'system'`, `autoSelectedViaModeration: true`) is written atomically alongside it. Concurrency safety (a manual `selectCustomerProfilePhoto` call racing this auto-selection) relies on Firestore's own optimistic-concurrency transaction retry, the same mechanism this document already cites for `completeCustomerProfile`. **New client reader, P.4.3B**: `CustomerPhotoGateway.watchSelectedProfilePhotoRef` (`lib/features/customer_photos/`) now also reads `selectedProfilePhotoRef` directly for the owning customer's own Profile hero (already permitted by this rule's `isTenantCustomer` branch — no rule change) — `resolveCustomerHeroPhoto` treats this projection as the sole "approved + selected" authority, never `CustomerPhoto.isSelectedAsProfilePhoto` in isolation. |
| `customerPhotoUploadGrants/{grantId}` | generated id | `organizationId` (own field) | Cloud Function only — `requestCustomerPhotoUploadGrant` (create); consumed/read cross-service by `storage.rules`, never by a client `get()` | P.4.2A. Short-lived, server-issued authorization for exactly one Storage object path. Fields: `customerId`, `organizationId`, `objectPath`, `contentType`, `requestKey` (replay-idempotency key), `expiresAt`. **New field, Customer Registration CR.1.2**: `purpose` (nullable, same closed enum as `customerPhotos.purpose` above) — optional on the request, validated server-side against a closed allow-list (`invalid-argument` for anything else), stored on the grant, and folded into the existing `requestKey`-replay comparison (a replay that tries to change `purpose` on a still-live grant fails `failed-precondition`, exactly like every other replayed-with-different-params case). Read: `allow read: if false` — the callable's own response already returns everything the client needs. Write: `allow write: if false` — no client write path; a client cannot forge its own upload authorization or its `purpose`. |

| `loyaltyLedgerEntries/{entryId}` | **deterministic hash**, `` `loyalty-${sha256Hex(`${organizationId}|${customerId}|${entryType}|${sourceId}`)}` `` (`deriveLoyaltyLedgerEntryId`, `functions/src/loyaltyLedger.ts`) — tenant + customer + entry-type + source-scoped in one hash, never a naked `{orderId}-earn`-style string, reusing the real, already-exported `sha256Hex` (`submitTakeawayOrder.ts`) rather than a third local duplicate | `organizationId` | **P1 (2026-08-20) — rules/index live.** `firestore.rules` denies all client writes (`allow write: if false`) and gates read to `isRealCustomerAuth() && isOwner(resource.data.customerId) && isTenantCustomer(resource.data.organizationId)` — owner identity ALONE is not tenant authorization (a Firebase Auth uid is global and could, once real multi-tenant membership exists, own records in more than one organization), so read additionally requires a genuine `tenantCustomers/{organizationId}_{uid}` membership document for the record's own `organizationId` specifically (same-day security correction; `isTenantCustomer` reused verbatim from its existing `customerPublicProfiles` use, not a new rule pattern). No staff/org-member branch (deferred, future permission model). Composite index `organizationId ASC, customerId ASC, createdAt DESC` added. **P2A (2026-08-20) — first real writer.** `functions/src/loyaltyOrderEarning.ts`'s `onOrderEventCreatedForLoyaltyEarning` (a `orderEvents` outbox-consumer trigger, not a client) writes `entryType: 'orderEarn'` entries — one per completed, eligible-channel (`takeaway`\|`delivery`\|`reservationPreorder` only, see `BR-LOYALTY-012`), non-guest order, `tx.create()`-d atomically alongside the matching `loyaltyAccounts` mutation. **P4-B (2026-08-22) — second real writer, `entryType: 'boncukRedemption'`.** `submitTakeawayOrder.ts`'s authenticated-path transaction writes a `boncukRedemption` entry (`entitlementDeltaBoncuk: 0`, `spendableDeltaBoncuk: -boncukUsed`, `debtDeltaBoncuk: 0`) synchronously, in the SAME transaction that creates the order and debits `loyaltyAccounts.spendableBalance` — unlike `orderEarn`'s async outbox-consumer pattern, redemption cannot defer its account mutation, since the double-spend guarantee (`BR-LOYALTY-019`) depends on the balance check and the debit happening inside one atomic transaction. Deterministic id (`deriveLoyaltyLedgerEntryId`, `sourceId: orderId`) gives it the same natural once-only guarantee `orderEarn` already has via `tx.create()`. **P4-C-B (2026-08-22) — third real writer, `entryType: 'boncukRedemptionRestore'`.** `functions/src/loyaltyRedemptionRestore.ts`'s consumer (reacts to `orderEvents`' new `order.rejected`/`order.cancelled`/`order.refunded` types) writes a `boncukRedemptionRestore` entry — `entitlementDeltaBoncuk: 0` always, but `spendableDeltaBoncuk`/`debtDeltaBoncuk` are the ACTUAL debt-first effects (`BR-LOYALTY-020`), never a naive `+boncukUsed`/`0` pair: an earlier direct-credit design was reviewed and rejected for letting `spendableBalance` and `boncukDebt` be simultaneously positive. `reversalOf` points at the original `boncukRedemption` entry's own document id; `redemptionValueMinorUnitsPerBoncuk`/`maxRedemptionBasisPoints`/`loyaltyPolicyVersion`/`amountBasisMinorUnits` are copied verbatim from that original entry, never re-resolved from whatever policy is active at restoration time (policy-version-safe by construction — the restored Boncuk COUNT itself comes directly from the original entry's own `spendableDeltaBoncuk`). Same deterministic-id + `tx.create()` idempotency mechanism as every other entry type. **P4-D-B (2026-08-22) — fourth real writer, `entryType: 'orderEarnReversal'`.** `functions/src/orderEarnReversal.ts`'s consumer (a SECOND, independent consumer of the same `order.refunded` outbox event `loyaltyRedemptionRestore.ts` also reacts to) writes an `orderEarnReversal` entry using the existing, already-proven O(1) `calculateFullOrderEarningReversal` (`loyaltyReversalMath.ts`) verbatim — never a ledger replay, never today's policy. `entitlementDeltaBoncuk`/`spendableDeltaBoncuk`/`debtDeltaBoncuk` are the math's own `requiredClawbackBoncuk`/`spendableRemovedBoncuk`/`debtIncreaseBoncuk` outputs (negated per the established `0 - x` convention where applicable); `reversalOf` points at the original `orderEarn` entry's own document id; every provenance field (`amountBasisMinorUnits`/`earningSpendMinorUnits`/`earningBoncukAmount`/`loyaltyPolicyVersion`) is copied verbatim from that original entry. Safe ONLY because `loyaltyOrderEarning.ts`'s own earning transaction now re-checks the canonical order's status for `"refunded"` INSIDE its own transaction before ever creating an `orderEarn` entry (the completed → refunded async race closure) — this is what makes "no original `orderEarn` entry exists" a permanently safe no-op here, not merely a convenient one; see `BR-LOYALTY-022`. Every other `entryType` in the closed union remains unwritten (reward-catalog/wheel/task/admin — all future phases). | **Boncuk Loyalty Program (`docs/decisions.md` P0-A/P1/P2A, `docs/business_rules.md` `BR-LOYALTY-*`).** Append-only, immutable — the sole source of truth for every Boncuk-affecting event, mirroring `auditEvents`'/`orderEvents`' own append-only discipline. Fields (contract defined in `functions/src/loyaltyLedger.ts`'s `LoyaltyLedgerEntry`; `orderEarn` is the one entry type actually written, by P2A): `organizationId`, `customerId`, `entryType` (closed camelCase enum: `orderEarn`\|`orderEarnReversal`\|`boncukRedemption`\|`boncukRedemptionRestore`\|`catalogRedemption`\|`catalogRedemptionRestore`\|`wheelEarn`\|`wheelExpiry`\|`taskEarn`\|`taskReversal`\|`adminAdjustment`), `deltaBoncuk` (signed int), `sourceId` (generic, `entryType`-dependent pointer), `orderId` (nullable, denormalized separately from `sourceId` for order-scoped reconciliation queries), `amountBasisMinorUnits` (nullable, spend-derived earn types only), `earningCarryNumeratorBefore`/`earningCarryDenominatorBefore`/`earningCarryNumeratorAfter`/`earningCarryDenominatorAfter` (nullable strings, `orderEarn`/`orderEarnReversal` only — the exact Boncuk-fraction carry before/after this event, see the Fractional Entitlement Carry note below), `createdAt`, `idempotencyKey`, `reversalOf` (nullable, points at the original entry's id), `expiresAt` (nullable, `wheelEarn` only), `metadata` (closed per-`entryType`-documented discriminated-union shape — `LoyaltyLedgerMetadata` — never an arbitrary `Map`). Never written independently of `loyaltyAccounts` — every future write path updates both inside one transaction (see `docs/decisions.md` P0-A for the full reasoning). **P2B-B (2026-08-22) — IMPLEMENTED**, superseding the P2B-A.1 design draft (which used `aggregateBefore/AfterMinorUnits`/`entitlementBefore/AfterBoncuk` — renamed for clarity in the real implementation, meanings unchanged): `deltaBoncuk` is removed entirely, replaced by three always-populated, never-null signed fields — `entitlementDeltaBoncuk`/`spendableDeltaBoncuk`/`debtDeltaBoncuk` (`BR-LOYALTY-016`) — since a single delta cannot unambiguously represent an event (e.g. a reversal) that changes gross entitlement, spendable balance, and debt by three different amounts at once. `orderEarn`/`orderEarnReversal` share nullable top-level provenance fields: `orderEligibleNetSpendBeforeMinorUnits`/`orderEligibleNetSpendAfterMinorUnits`, `orderEntitlementBeforeBoncuk`/`orderEntitlementAfterBoncuk`, `debtBeforeBoncuk`/`debtAfterBoncuk` (alongside the pre-existing `remainderBeforeMinorUnits`/`remainderAfterMinorUnits`) — no `LoyaltyLedgerMetadata` variant needed for either. `orderEarn` is written by `functions/src/loyaltyOrderEarning.ts`'s aggregate/debt-aware earning transaction; `orderEarnReversal` has a tested, pure calculation (`functions/src/loyaltyReversalMath.ts`) but **no writer** — see `docs/decisions.md`'s P2B-B entry for the full worked example and gate totals. **P3A (2026-08-23) — first real reader.** `getCustomerLoyaltyHistory` (`functions/src/getCustomerLoyaltyHistory.ts`) is the first callable to read this collection for customer-facing use — gated identically to `getCustomerLoyaltySnapshot` (real-phone-customer check, server-derived `organizationId`, independently re-verified `tenantCustomers` membership), bounded/paginated (`.where('organizationId','==',organizationId).where('customerId','==',uid).orderBy('createdAt','desc').limit(pageSize<=30)`, single-field `startAfter` cursor). Returns a narrow, sanitized projection only — `eventId`/`type`/`displayBoncukDelta`/`debtAppliedBoncuk`/`occurredAt`/`orderId` (`BR-LOYALTY-017`) — never the raw document, so this collection's internal accounting fields (`entitlementDeltaBoncuk`/`spendableDeltaBoncuk`/`debtDeltaBoncuk`/`sourceId`/`idempotencyKey`/`metadata`/etc.) are never exposed to a client. No direct client Firestore query against this collection exists anywhere — the callable is the only read path a client ever takes. Consumed by the real (no-longer-mock) `lib/features/loyalty/presentation/screens/loyalty_history_screen.dart`. **Configurable Loyalty Economics (2026-08-24) — new field `loyaltyPolicyVersion` (nullable int).** The rate fields are no longer snapshots of a fixed constant — they are the resolved value of the organization's real `loyaltyPolicies/{organizationId}` document (`functions/src/loyaltyPolicy.ts`) at write time. `loyaltyPolicyVersion` is the direct link back to the exact `loyaltyPolicyVersions/{organizationId}_{version}` record that produced this entry, populated on every `orderEarn` write going forward (`null` only for an entry written before this field existed — none exist in practice, since this collection has no production data yet). A future policy change never rewrites an existing entry's snapshotted rate/version — see `BR-LOYALTY-018`. **Exact-ratio snapshot, redemption provenance.** `earningRateMinorUnitsPerBoncuk` (a single derived per-Boncuk rate) never shipped to `main` — it is replaced by two always-populated-together raw fields on every `orderEarn` entry, `earningSpendMinorUnits`/`earningBoncukAmount`, snapshotted verbatim from the resolved policy at write time (exact-ratio `BigInt` math, no divisibility assumption). `redemptionRateMinorUnitsPerBoncuk` is renamed `redemptionValueMinorUnitsPerBoncuk` (naming consistency with `loyaltyPolicies`, no writer yet — reserved for a future redemption entry type), and a new nullable `maxRedemptionBasisPoints` field is reserved alongside it for the same future writer. **Fractional Entitlement Carry correction — replaces the rejected per-policy-version "earning epoch" design.** The currency-denominated, epoch-scoped fields this row previously described
(`orderEligibleNetSpendBeforeMinorUnits`/`orderEligibleNetSpendAfterMinorUnits`/`orderEntitlementBeforeBoncuk`/`orderEntitlementAfterBoncuk`/`remainderBeforeMinorUnits`/`remainderAfterMinorUnits`/`earningEpochReset`) are REMOVED — that design was rejected for forfeiting a customer's economically-earned partial progress on a policy change, even though it never re-rated old spend. They are replaced by four fields expressing the customer's carried-forward partial progress as an EXACT, POLICY-INDEPENDENT FRACTION of one Boncuk: `earningCarryNumeratorBefore`/`earningCarryDenominatorBefore`/`earningCarryNumeratorAfter`/`earningCarryDenominatorAfter` — canonical non-negative-integer DECIMAL STRINGS (never `Number`; a carry denominator can exceed `Number.MAX_SAFE_INTEGER` after a handful of policy changes), snapshotting the customer's exact `loyaltyAccounts.earningCarryNumerator`/`earningCarryDenominator` immediately before and after this specific event. A policy change never rewrites these fields on any existing entry — it only changes what a NEW entry will compute (`functions/src/loyaltyPolicy.ts`'s `combineCarryWithEarning`; see `BR-LOYALTY-018`). |
| `loyaltyAccounts/{organizationId}_{uid}` | composite | `organizationId` | **P1 (2026-08-20) — real, implemented.** `getCustomerLoyaltySnapshot` (`functions/src/getCustomerLoyaltySnapshot.ts`) is a narrow, trusted callable that resolves `organizationId` exclusively server-side (never accepted from `request.data`), independently re-verifies `tenantCustomers` membership via the Admin SDK, and idempotently creates a zero-state account on first call (`tx.set` inside a transaction that re-reads to stay safe under a concurrent first call), never resetting an existing account on a repeat call. **P2A (2026-08-20) — second writer.** `onOrderEventCreatedForLoyaltyEarning` (`functions/src/loyaltyOrderEarning.ts`) also provisions-or-updates this document, inside the same transaction as its matching `loyaltyLedgerEntries` write: `spendableBalance`/`lifetimeEarned` incremented, `earningCarryNumerator`/`earningCarryDenominator` replaced (Fractional Entitlement Carry correction — see below), `lifetimeRedeemed`/`createdAt` untouched, `revision` incremented. Both writers share the exact same `LoyaltyAccountData` shape (exported from `getCustomerLoyaltySnapshot.ts`, imported rather than duplicated) and the same `{organizationId}_{uid}` document id, so they can never diverge on shape or targeting. `firestore.rules`: `allow write: if false`; read gated to `isRealCustomerAuth() && isOwner(resource.data.customerId) && isTenantCustomer(resource.data.organizationId)` — deliberately narrower than `customerPhotos`'s blanket `isOrgMember` staff-read, since balance is money-equivalent value; `isRealCustomerAuth()` (reused from the existing Table Guest Session boundary) excludes a guest/anonymous Firebase Auth session, and `isTenantCustomer(resource.data.organizationId)` (reused verbatim from its existing `customerPublicProfiles` use) is required in addition to owner-uid-match — **owner identity alone is not tenant authorization**: a Firebase Auth uid is global, so the same uid could (once real multi-tenant customer membership exists) legitimately own a loyalty account in more than one organization, and `customerId == request.auth.uid` alone would let a customer read an account of theirs in a tenant they no longer (or never did) belong to. Same-day security correction — see `docs/decisions.md`'s P1 entry. Staff/admin read intentionally not scaffolded until a specific permission model is decided. | **Boncuk Loyalty Program.** A server-maintained **materialized view** for fast balance reads — never an independent source of truth; always written atomically alongside its corresponding `loyaltyLedgerEntries` entry, in the same transaction (true since P2A's `orderEarn` writer; the same discipline binds every future ledger writer too), so it can never drift from the ledger (mirrors `customerPublicProfiles.selectedProfilePhotoRef`'s own atomically-co-written-projection precedent, and `reservationSlotOccupancy`'s bucketed-counter precedent). Fields: `organizationId`, `customerId`, `spendableBalance` (int, whole Boncuk), `boncukDebt` (int, `>= 0`), `validOrderEntitlementBoncuk` (int, `>= 0` — the currently valid whole Boncuk entitlement from non-reversed order spend; the O(1) reversal projection, see the O(1) reversal note below — NOT `lifetimeEarned`, which is monotonic and never decremented by a reversal), `earningCarryNumerator`/`earningCarryDenominator` (strings — the exact, policy-independent carried-forward Boncuk-fraction `BR-LOYALTY-001`/`BR-LOYALTY-018` require; see the Fractional Entitlement Carry note below), `lifetimeEarned`/`lifetimeRedeemed` (int, monotonic), `createdAt` (immutable after creation), `updatedAt`, `revision` (int, optimistic-concurrency version, mirrors `StaffMember`/`CustomerPhoto`/`ReservationPolicy`'s own established `revision` convention — stays `1` until a real earning/redemption mutation occurs — P2A's `orderEarn` writer is the first to increment it; a pure snapshot read never bumps it). Composite id mirrors `tenantCustomers`/`customerPublicProfiles`'s exact convention. **Deliberately never extends `tenantCustomers`** — see `docs/decisions.md` P0-A for why a dedicated collection was chosen over overloading the membership/identity document. Permanently separate from CRM Visit Passport data (`BR-CRM-008`) — no shared balance, ledger, or field with `tenantCustomers`' own aspirational "reward history." **P2B-B (2026-08-22) — IMPLEMENTED, later superseded by the Fractional Entitlement Carry correction**: introduced `boncukDebt` (int, `>= 0` — absorbs any refund clawback that exceeds `spendableBalance`, paid down by future earning before any of it becomes spendable, BR-LOYALTY-014, still current) and an `orderEligibleNetSpendMinorUnits` currency aggregate (since REMOVED — see below). `spendableBalance >= 0` is a hard invariant — never negative, by construction (excess clawback goes to `boncukDebt` instead). `getCustomerLoyaltySnapshot`'s response includes `boncukDebt` (never the raw carry fields, which stay internal). See `docs/decisions.md`'s P2B-A/P2B-A.1/P2B-B entries for the original reversal-formula design history, and its Fractional Entitlement Carry correction entry for the current formula (`functions/src/loyaltyReversalMath.ts` — still a pure, unwired calculation; no refund writer exists yet). **P3A (2026-08-23) — first real client consumer.** `getCustomerLoyaltySnapshot`'s existing response (`spendableBalance`/`boncukDebt`/`earningRemainderMinorUnits`/`lifetimeEarned`/`lifetimeRedeemed`) is now actually rendered by a real screen for the first time — `lib/features/loyalty/presentation/screens/loyalty_screen.dart` (canonical, replacing the previous entirely-mock `features/profile/...loyalty_screen.dart`), via `LoyaltyAccountSnapshot`/`FirebaseLoyaltyGateway`/`loyaltySnapshotProvider` (`lib/features/loyalty/{domain,data,presentation}/**`). `earningRemainderMinorUnits` (already present since P2B-B, later redefined by the Fractional Entitlement Carry correction as a fresh-on-every-read carry projection rather than a stored value) is what the screen's "Sonraki Boncuğa" progress card is computed from — never recalculated from order history on-device, and never hardcodes the denominator. **Configurable Loyalty Economics (2026-08-24) — `LoyaltyAccountSnapshot`'s policy fields are now real server-resolved instance data, not Flutter constants; see the new `loyaltyPolicies`/`loyaltyPolicyVersions`/`loyaltyPolicyBootstraps` rows below and `BR-LOYALTY-018`.** **Fractional Entitlement Carry correction — the final, accepted accumulation model, replacing a rejected per-policy-version "earning epoch" design.** The rejected design added `earningPolicyVersion`/`orderEligibleNetSpendMinorUnits`/`earningRemainderMinorUnits` fields and reset the aggregate to `0` on a policy-version change — this was reviewed and rejected because it FORFEITED a customer's real, economically-earned partial progress on a policy change, even though it never re-rated old spend under the new ratio. None of those three fields exist in the shipped schema. In their place: `earningCarryNumerator`/`earningCarryDenominator` (canonical non-negative-integer decimal strings — see the `loyaltyLedgerEntries` row above for why never `Number`) represent the customer's unconverted partial progress as an EXACT, POLICY-INDEPENDENT FRACTION of one Boncuk. Every earning event, under whatever policy is active at the time, combines the account's EXISTING carry with the new order's own exact fractional entitlement (`functions/src/loyaltyPolicy.ts`'s `combineCarryWithEarning`) — there is no reset, no epoch, and a policy change never touches an existing account's carry at all; it only supplies the rate the NEXT earning event uses for its own new spend. `getCustomerLoyaltySnapshot`'s response gained `minorUnitsUntilNextBoncuk` (alongside the existing `earningRemainderMinorUnits`) — both are a customer-safe minor-unit PROJECTION of the exact stored carry against whichever policy is currently active (`projectCarryToPolicyProgress`), computed fresh on every read, never cached, never a reinterpretation of history. See `BR-LOYALTY-018` for the full model, worked example, and the five `OLD_PROGRESS_*` guarantees this satisfies. **O(1) reversal correction — new field `validOrderEntitlementBoncuk`.** An earlier same-week draft reversed an order by restoring its own snapshotted carry and REPLAYING every later `orderEarn` entry the customer had since earned — mathematically correct but operationally UNBOUNDED (a refund of an old order could require scanning/replaying thousands of later entries), rejected before merge. `validOrderEntitlementBoncuk` is incremented by exactly the whole Boncuk each earning event produces (the same value already computed for the ledger/debt logic — no extra cost); a full reversal then needs only this field + the current carry (one account read) plus the ONE reversed order's own immutable ledger entry (one more read) — no later entry is ever read or iterated. Proven mathematically identical to a full replay by `combineCarryWithEarning`'s own telescoping property — see `BR-LOYALTY-018` and `functions/src/loyaltyReversalMath.ts`'s own doc comment for the full derivation. **P4-B (2026-08-22) — third writer, checkout redemption debits (takeaway only).** `submitTakeawayOrder.ts`'s authenticated-path transaction also writes this document: `spendableBalance` decremented by `boncukUsed`, `lifetimeRedeemed` incremented by the same amount, `revision` incremented — `boncukDebt`/`validOrderEntitlementBoncuk`/`earningCarryNumerator`/`earningCarryDenominator`/`lifetimeEarned`/`createdAt` all carried through unchanged (redemption never touches earning-side or debt state). Full-document `tx.set()`, matching `loyaltyOrderEarning.ts`'s own overwrite convention for this financially-sensitive document rather than a partial `merge` write. A missing account at redemption time is rejected outright (`failed-precondition`) rather than implicitly treated as a zero balance — see `functions/src/loyaltyRedemption.ts`'s `resolveAccountForRedemption` and `BR-LOYALTY-019`. **P4-C-B (2026-08-22) — fourth writer, redemption restoration (`BR-LOYALTY-020`).** `functions/src/loyaltyRedemptionRestore.ts`'s consumer also writes this document: `spendableBalance`/`boncukDebt` updated via the exact same debt-first split as its own ledger entry (never a naive full credit to `spendableBalance`), `revision` incremented, `lifetimeRedeemed`/`lifetimeEarned`/`validOrderEntitlementBoncuk`/`earningCarryNumerator`/`earningCarryDenominator`/`createdAt` all preserved exactly. The debt-first formula itself now lives in the shared, channel-neutral `functions/src/loyaltyAccounting.ts` (`applyBoncukCreditDebtFirst`) — both this writer and `loyaltyOrderEarning.ts`'s own earning writer call the identical primitive, never two independently-maintained copies of the same rule. |
| `loyaltyPolicies/{organizationId}` | the organization's own id (single current-policy document per org) | own field | **Configurable Loyalty Economics (2026-08-24) — real, implemented (resolver/read path only).** Sole writer: `functions/src/loyaltyPolicy.ts`'s `resolveActiveLoyaltyPolicy`/`resolveActiveLoyaltyPolicyInTransaction` — auto-provisions the locked default economics (`DEFAULT_LOYALTY_POLICY_ECONOMICS`) transactionally on an organization's genuine first resolution (never on a resolution where a `loyaltyPolicyBootstraps/{organizationId}` marker already exists but the policy document doesn't — that fails closed instead, see the `loyaltyPolicyBootstraps` row below), mirroring `loyaltyAccounts`' own "plain read first, transaction only on first use" provisioning discipline. No `firestore.rules` entry exists — no rule means default-deny, mirroring `reservationNotificationDeliveries`' own established "no rule needed" precedent; the only client-facing exposure is the sanitized `SanitizedLoyaltyPolicy` projection `getCustomerLoyaltySnapshot` folds into its response (never the raw document). No Admin write callable exists yet — this collection has exactly one writer, firing at most once per organization, until that future phase. | **Boncuk Configurable Loyalty Economics.** The single current-active-policy materialized pointer — a denormalized, fast-read cache of the organization's real economics, always consistent with the latest entry in `loyaltyPolicyVersions` (never an independent source of truth for what "the policy" is, mirroring `loyaltyAccounts`' own relationship to `loyaltyLedgerEntries`). Fields: `organizationId`, `earningSpendMinorUnits`/`earningBoncukAmount` (int — eligible net spend, in minor units, that earns `earningBoncukAmount` whole Boncuk; both validated only as positive integers, `isValidLoyaltyPolicyEconomics`), `redemptionValueMinorUnitsPerBoncuk` (int, BR-LOYALTY-005), `maxRedemptionBasisPoints` (int, `0`–`10000`), `version` (int, starts at `1`), `effectiveAt`/`createdAt`/`updatedAt` (Timestamps). Locked default: `5000`/`5`/`100`/`5000` (50 TL = 5 Boncuk, 1 Boncuk = 1 TL, 50% max) — this is INITIAL policy data seeded on first genuine provisioning, not a permanent constant. **Same-day correction (2026-08-24)**: an earlier same-day draft of this row required `earningSpendMinorUnits` to divide evenly by `earningBoncukAmount`; that constraint was rejected before merge as insufficient for arbitrary Admin-configured ratios (e.g. `5000 → 3`) and does not exist in the shipped validator — see `BR-LOYALTY-018`. See `BR-LOYALTY-018` and `docs/decisions.md`'s Configurable Loyalty Economics entry. |
| `loyaltyPolicyVersions/{organizationId}_{version}` | deterministic composite (`{organizationId}_{version}`) | own field | **Configurable Loyalty Economics (2026-08-24) — real, implemented.** Written atomically alongside `loyaltyPolicies/{organizationId}`'s own first-provisioning write, inside the same transaction — the two can never diverge on their first (default) version. No `firestore.rules` entry — default-deny, not client-readable at all (not even via a sanitized projection; this collection is a pure backend audit trail). | **Boncuk Configurable Loyalty Economics.** Append-only, immutable — the permanent historical record of every policy version an organization ever had active, mirroring `loyaltyLedgerEntries`'/`auditEvents`' own append-only discipline. Fields: same four economics fields as `loyaltyPolicies` plus `organizationId`/`version`/`effectiveAt`/`createdAt` — deliberately no `updatedAt` (a version record is never updated after creation). Exists specifically so a future Admin policy change has a genuine audit trail independent of the ledger's own per-entry rate snapshots (`BR-LOYALTY-018`'s own doc comment explains why the ledger's snapshot fields are not a substitute for this collection). Only one version (`_1`, the locked default) exists anywhere today — no Admin write path has ever created a second. |
| `loyaltyPolicyBootstraps/{organizationId}` | the organization's own id | own field | **Same-day correction (2026-08-24) — real, implemented.** Written atomically alongside `loyaltyPolicies/{organizationId}`'s and `loyaltyPolicyVersions/{organizationId}_1`'s own first-provisioning writes, inside the same transaction (`tx.create()` for all three — never re-created, never updated, never deleted by any code path). No `firestore.rules` entry — default-deny, not client-readable or writable at all; a pure internal marker. | **Boncuk Configurable Loyalty Economics — missing-vs-first-time-provisioning boundary.** A permanent, minimal marker (`organizationId`, `bootstrappedAt`) whose sole purpose is disambiguating two otherwise-identical-looking states when `loyaltyPolicies/{organizationId}` is absent: genuine first-time use (no bootstrap marker either — safe to auto-provision the locked default) versus data loss/corruption of a policy that was already live (bootstrap marker exists, policy document doesn't — resolution fails closed, `missing-live-policy`/`missing-live-loyalty-policy`, and the default is never silently recreated, since that could unexpectedly change an organization's live loyalty economics back to the default after it had been configured to something else). See `BR-LOYALTY-018`. |

**Deliberately not modeled as separate collections**: `staffMembers`/`platformMembers`' role-change
audit trail reuses `auditEvents` (tagged by a `domain`/`type` field) rather than a dedicated collection
— mirrors the existing Dart codebase's own established "one shared audit repository can cover several
sub-concepts within one bounded context" precedent (Phase 8's `MarketplaceAuditEntry`).

## Required guarantees → rule mechanism (cross-reference)

| Requirement | `firestore.rules` mechanism |
|---|---|
| Clients cannot assign themselves an organization | `memberships`/`staffMembers`/`organizations` collections have no client `create`/`update` rule at all — Cloud Functions use the Admin SDK, which bypasses rules entirely; this is not a rule that could be bypassed, there is structurally no client write path. |
| Clients cannot promote roles | Same mechanism — `roles`/`organizationAccess`/`branchAccess`/`restaurantAccess` fields are never client-writable on any document. |
| Clients cannot write arbitrary entitlement grants | `entitlements` collection: read-only for clients. |
| No trust of caller-supplied tenant scope | Every rule's authorization decision reads `request.auth.token`/`memberships`, never `request.resource.data.organizationId` — that field is validated (must match), never trusted as the source of the decision. |
| Every protected read/write validates membership | `isOrgMember(organizationId)` helper, called by every collection's rules. |
| Restaurant scope resolves to organization | `restaurants/{id}.organizationId` is the resolution; `branches` denormalize it, verified at create. **Faz D.1.1**: this is now a real, tested mechanism (`provisionRestaurant`, `functions/src/test/provisioning.test.ts`) — a restaurant claiming an `organizationId` that was never provisioned, or one that has been deactivated, is rejected before any document is written. Closes Faz D.1's REQUIRED finding. |
| Branch scope resolves through branch → restaurant → organization | Verified once at `branches` document creation via `get()` on the parent `restaurants` document; immutable thereafter. **Faz D.1**: this is now a real, tested mechanism (`provisionBranch`, `functions/src/test/provisioning.test.ts`), not just documented intent — a caller cannot provision a branch under a restaurant that belongs to a different organization than claimed. |
| Platform Owner and tenant authorization remain separate | Separate custom-claim namespace (`platformRole` vs. `roles`/`organizationAccess`), separate collections, no rule ever grants a platform action via a tenant claim or vice versa. |
| Support access is temporary, justified, audited | `supportGrants/{grantId}` collection (`organizationId`, `platformMemberId`, `reason`, `expiresAt`) — a platform member's elevated read access to `organizationId` is only granted while a matching, non-expired `supportGrants` document exists; every grant is itself an `auditEvents` entry. |
| Cross-tenant queries fail closed | Firestore's own rule semantics: a `list`/query rule requiring `resource.data.organizationId in request.auth.token.organizationAccess` rejects the **entire** query if any possible result could fail it — there is no partial/leaky result set. |
| Backups/restores must not mix tenants | Not a rule concern — named in `docs/phase9_architecture_analysis.md` §16 as real, deferred Sprint 9J-adjacent work (Firestore's native export is collection-level, not tenant-level). |

## What this sprint (9B) does NOT do

- Deploy these rules to any real Firebase project (`abakusone`/`abakus-one-dev`/`abakus-one-staging`) —
  emulator-verified only, per the kickoff's own stop condition on requiring real console/deployment
  access.
- ~~Build the Cloud Function that syncs `memberships` writes into custom claims — that's Sprint 9F.
  Until it exists, custom claims must be set manually for any emulator test or real pilot use.~~ **No
  longer fully true**: Faz R.3A built `syncOwnStaffClaims`, a client-invoked callable (not the
  automatic write-trigger this bullet originally envisioned) — see "Custom claims vs. Firestore
  lookups" above for the exact mechanism. Manual claims-setting remains necessary only in tests that
  deliberately bypass the real pipeline for speed/isolation (`mintStaffIdToken`-style helpers already
  used throughout `functions/src/test/*.test.ts`).
- Populate `firestore.indexes.json` with real compound indexes — no concrete multi-field query exists
  in the app yet (Sprint 9E introduces the first real Firestore-backed repositories); indexes are added
  when a real query that needs one actually exists, never spéculatively.
- Migrate any real repository off `InMemory*` onto these collections — that's Sprint 9E.

## Faz P.1/P.2 (Paket Servis / Delivery foundation) — deliberately not modeled here yet

- **`customerAddresses` is now real as of Faz P.2** — see the Collections table above. Faz P.1
  deliberately deferred it (no provider, no UI consumer yet); Faz P.2 introduced the real Google
  Places API (New) verification provider and built it for real, matching the design this note
  originally sketched: customer CRUD on their own records only, client-written coordinates/address
  components never trusted as authority (`saveDeliveryAddress` independently re-resolves every save
  server-side).
- **No delivery-zone/eligibility collections.** `resolveDeliveryEligibility` (a future preview
  callable) and any authoritative zone/neighborhood records are Faz P.3 scope, not modeled here.
- **`channelPricingPolicies/{restaurantId}`'s delivery entries are not written by this phase.** The
  LOCKED delivery pricing rule (+140 TL / +20 TL beverage) is proven correct
  (`lib/features/menu/domain/pricing/delivery_channel_pricing_policy.dart`,
  `functions/src/test/deliveryPricing.test.ts`) but deliberately kept out of the live
  `InMemoryChannelPricingPolicyRepository` seed that `tool/export_menu_catalog.dart` →
  `catalogMigration.ts` would otherwise migrate into this real document — see `docs/decisions.md`
  Faz P.1 D4 for the live-UI-safety reasoning. A future phase wiring real delivery checkout will seed
  this collection's `delivery` entries deliberately, not as a side effect of an unrelated migration
  run.
