/// A [CustomerFeedback] ticket's triage priority — Sprint 5D's Customer
/// Feedback Center. `medium` is the default an administrator hasn't yet
/// reviewed and re-prioritized.
enum FeedbackPriority {
  low,
  medium,
  high,
  urgent,
}
