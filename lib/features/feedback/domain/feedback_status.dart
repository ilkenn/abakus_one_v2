/// A [CustomerFeedback] ticket's triage status — Sprint 5D's Customer
/// Feedback Center. `open` is always the initial status a submission
/// starts at.
enum FeedbackStatus {
  open,
  inReview,
  resolved,
  closed,
}
