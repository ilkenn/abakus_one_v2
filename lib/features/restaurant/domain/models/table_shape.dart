/// The rendered shape of a [RestaurantTable] on a [FloorPlan]'s live map.
///
/// Purely a rendering/layout concern — has no effect on [TableStatus] or
/// any operational logic.
enum TableShape { round, square, rectangle }
