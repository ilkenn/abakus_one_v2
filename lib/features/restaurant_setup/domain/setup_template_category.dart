/// A restaurant-type category a `SetupTemplate` targets — Phase 7
/// (`docs/decisions.md` ADR-024).
enum SetupTemplateCategory {
  bowlAndSalad,
  burger,
  pizza,
  wrap,
  pasta,
  cafe,
  chicken,
  doner,
  sushi,
  bakeryDessert,
  smoothie,
  mixedRestaurant,
}
